# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
/dist
*.tsbuildinfo

# env / secrets  ⛔ never commit
.env
.env.*
!.env.example

# supabase local
/supabase/.branches
/supabase/.temp

# ⛔ รายชื่อ DB ที่เราถือ — มีรหัสผ่าน DB อยู่ในนั้น (ตัวอย่างอยู่ที่ targets.example.json)
/supabase/targets.json

# ⛔ ข้อมูลจริง (ใช้ยื่นภาษี) — ห้ามขึ้น git เด็ดขาด
/migration/export/
/migration/data/
/backup/
*.dump


# vercel
.vercel

# testing
/coverage

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
Thumbs.db
*.pem

.vercel
.env*
