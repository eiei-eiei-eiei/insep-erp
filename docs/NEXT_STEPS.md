# งานที่เหลือ — ส่งต่อให้ session ถัดไป

> เขียน 2026-08-01 (D43) · อัปเดต 2026-08-02 (D44) · 2026-08-11 (D45 + สถาปัตยกรรม productization)
> · 2026-08-12 (ย้าย DB จริงขึ้น 0032 · LINE ต่อ tenant · MFA ตกไป · 4.4+4.5)
> **อัปเดตล่าสุด 2026-08-14 — 4.3 VAT branching เสร็จ (0036 · D55) · กิจการไม่จด VAT ใช้ได้จริงแล้ว**
> **อ่านไฟล์นี้ก่อน** แล้วค่อยดู `CLAUDE.md` · `docs/DECISIONS.md` D46-D55 · `docs/DESIGN_SYSTEM.md`
> **แอปจัดการหลังบ้าน**: `docs/ADMIN_APP_REQUIREMENTS.md` — **เฟส 1 เสร็จแล้ว** (ดูข้อ 10)
> เฟส 2 (ตารางค่างวด) / เฟส 3 (เตือนอัตโนมัติ) ยังไม่เริ่ม

---

## 0. สถานะตอนนี้ (สำคัญ — อ่านก่อนทำอะไร)

### ⚠️ มี 2 DB และ 2 branch อยู่ตอนนี้ — สับสนแล้วพังของจริง

| | DB production (ของเจ้าของ) | DB แพลตฟอร์มลูกค้า (ใหม่) |
|---|---|---|
| ref | `vmhiwlxdyhatucioalzp` | `tnuxrufpzeyuvwdmkojv` |
| migration | ถึง **0034** ครบ | ถึง **0036** ครบ |
| ⏳ ค้างลง DB จริง | **0035 + 0036** ← ต้อง `db:push` ก่อน push โค้ด | — (ลงครบแล้ว) |
| ข้อมูล | ของจริงที่ใช้ยื่นภาษี | tenant สาธิต `rongkor` / `rongkhor` |
| บัญชี Supabase | บัญชีเดิม | **บัญชีใหม่** (CLI link อยู่ที่นี่) |

> ✅ **2026-08-12: ทั้งสอง DB อยู่ที่ 0032 เท่ากันแล้ว** — ข้อห้าม "ห้าม merge เข้า main" ยกเลิกแล้ว
> (เดิมห้ามเพราะ DB production ยังไม่มี `profiles.tenant_id` ที่โค้ดใหม่ต้องใช้ — ตอนนี้มีแล้ว)

- `supabase link` + `.env.local` ตอนนี้ชี้ **DB production** (`vmhiwlxdyhatucioalzp`)
  🚨 เช็คก่อน `db:push` ทุกครั้ง: `cat supabase/.temp/project-ref`
  · ไปทดสอบ: `npx supabase link --project-ref tnuxrufpzeyuvwdmkojv` + `cp .env.local.testing-backup .env.local`
  · กลับ DB จริง: `npx supabase link --project-ref vmhiwlxdyhatucioalzp` + `cp .env.local.production-backup .env.local`
  ⚠️ `npm run test:tenant` อ่าน `.env.tenant-test.local` แยกต่างหาก (ชี้ project ทดสอบเสมอ) — ไม่กระทบกัน
- **บัญชี Supabase**: บัญชีใหม่ถูกเชิญเข้า org เก่าแล้ว → CLI เห็นทั้ง 2 project ไม่ต้องสลับ login อีก
- **สำรอง DB ก่อนแตะ migration เสมอ**: `npx tsx scripts/backup-tables.ts --env=<ไฟล์ env> --out=<โฟลเดอร์นอก repo>`
  (`supabase db dump` ใช้ไม่ได้ — ต้องมี Docker/pg_dump ซึ่งเครื่องผู้ใช้ไม่มี)
  ของรอบย้าย: `D:\insep-erp-backup\2026-08-12-before-0025\`

| เรื่อง | สถานะ |
|---|---|
| build / lint | ✅ ผ่าน |
| `npm run test` (unit, ออฟไลน์) | ✅ **287** |
| `npm run test:tenant` (ยิง Supabase จริง) | ✅ **102** — ต้องมี `.env.tenant-test.local` |
| ตรวจโค้ดหา query ที่พังจากการเปลี่ยน PK | ✅ **ไม่พบจุดพัง** (D49) — จุดเสี่ยงที่เหลือเป็นของ 4.3 |
| ค่าที่ต้องตั้งใน production | ✅ ครบ (ดูข้อ 6 — เหลือ opening_balance ที่**จงใจไม่ใส่**) |

### กติกาการ deploy (เปลี่ยนแล้ว)
- **GitHub auto-deploy ทำงานแล้ว** — `git push origin main` แล้ว Vercel deploy production ต่อเอง
  **ไม่ต้อง `vercel --prod` แล้ว** (เอกสารเก่าบางที่ยังเขียนว่าต้องรันเอง — ยกเลิก)
- ⚠️ **ห้าม push เองโดยไม่ถาม** — ผู้ใช้ตรวจใน `npm run dev` ก่อนเสมอ และตอนนี้ push = ขึ้น production ทันที
- มี migration ใหม่เมื่อไหร่ → **ผู้ใช้ต้อง `npm run db:push` ก่อน push โค้ด** (ไม่งั้น production พังระหว่างรอ)

### ข้อจำกัดที่ต้องรู้
- **Claude ล็อกอินเข้าแอปเองไม่ได้** (ห้ามกรอกรหัสผ่าน) → ตรวจ UI หลัง login ต้องให้ผู้ใช้ดู
  สิ่งที่ตรวจเองได้: หน้า `/login`, ค่า CSS ที่คำนวณจริง (ผ่าน `javascript_tool`), console error, build/lint/test
- ผู้ใช้ **เขียนโค้ดไม่ได้** — อธิบายทีละขั้น ตอบไทย

---

## 1. ✅ ตัวบล็อกการขาย — เอกสารการค้ายังเป็นชื่อบริษัทผู้ใช้ → **แก้แล้ว (D44, 2026-08-02)**

`print.ts` ไม่มี constant `COMPANY` แล้ว — อ่านจากตาราง `entities` (migration 0023 เพิ่ม
`name_eng`/`phone`/`bank_line`) · เลือกกิจการที่ออกเอกสารด้วย `app_settings.sales_doc_entity`
· ตรรกะประกอบข้อความ + golden test อยู่ที่ `lib/sales/company.ts` · UI = บัญชี → ตั้งค่า →
การ์ด "ข้อมูลบนเอกสารการค้า" (มีตัวอย่างหัวกระดาษจริงใต้ฟอร์ม) · รายละเอียดใน `docs/DECISIONS.md` D44

**ยังต้องทำ (ผู้ใช้)**:
1. ~~`npm run db:push`~~ → **รันแล้ว** (0023 อยู่ใน DB production ตั้งแต่ก่อน 2026-08-11 · บรรทัดเดิมล้าสมัย)
2. กรอกข้อมูลกิจการในการ์ดใหม่ (ค่าเดิมไม่ได้ย้ายให้อัตโนมัติ — ตั้งใจไม่ seed ข้อมูลบริษัทจริงลง repo ที่จะขายต่อ)
3. **พิมพ์เทียบใบเก่าวางทับ** ให้ครบทุกบรรทัด (ดู `docs/TESTING.md` ส่วนที่ 24)

**ยังไม่ทำ**: โลโก้บนหัวเอกสาร (รอผู้ใช้มีโลโก้ — ทำแล้ว layout ขยับ ต้องเทสรอบใหม่ · ดูข้อ 3)

---

## 2. ✅ white-label vs co-brand → **เคาะแล้ว: co-brand** (2026-08-11) · ทำเสร็จแล้ว

หน้า `/login` อ่านแบรนด์ลูกค้าจาก subdomain ได้แล้ว (view `tenant_branding` — D46/D47)
ไม่มี subdomain = หน้าตาเดิมเป๊ะ · มี subdomain = โลโก้/ชื่อลูกค้า + `powered by [ชื่อสินค้า]`

**ชื่อสินค้ายังเป็น TBC** — ตอนนี้ใช้ค่าคงที่ `PRODUCT_NAME` ใน `lib/shared/branding.ts`
เปลี่ยนที่บรรทัดเดียวจบ

<details><summary>บันทึกเดิม (เก็บไว้ดูเหตุผล)</summary>

หน้า `/login` อ่านชื่อแบรนด์จาก DB ไม่ได้ (ยังไม่ login = RLS บล็อก `app_settings`)
ตอนนี้ hardcode `"Insep ERP"` + `"ระบบภายในโรงกลั่นสุราคราฟต์"` ใน `app/(auth)/login/page.tsx`

| โมเดล | ลูกค้าเห็น | ผล |
|---|---|---|
| **A. White-label เต็ม** | แบรนด์ตัวเองล้วน ตั้งแต่หน้า login | ขายได้ราคาสูงกว่า · ผู้ใช้ไม่ได้ exposure |
| **B. Co-brand** (Claude แนะนำ) | โลโก้ลูกค้าเด่น + `powered by [ชื่อสินค้า]` เล็ก ๆ | ได้บอกต่อฟรีทุกวัน · ตลาดโรงคราฟต์ไทยรู้จักกันหมด |

ถ้าเลือก A: ชื่อ/โลโก้หน้า login ต้องมาจาก **env var ต่อ deployment** (`NEXT_PUBLIC_BRAND_NAME`)
เพราะอ่าน DB ไม่ได้ก่อน login

**ชื่อสินค้ายังเป็น TBC** — ผู้ใช้ยังไม่เคาะ (เคยใช้ชื่อสมมติว่า "กลั่น")

</details>

---

## 3. โลโก้ — **ผู้ใช้ไม่มีโลโก้บริษัท แต่ลูกค้าที่ซื้อโปรแกรมอาจมี**

> ผู้ใช้ยืนยัน 2026-08-02: ตัวเองไม่มีโลโก้ → **ห้ามทำให้เอกสารของผู้ใช้เปลี่ยนหน้าตา**
> แต่ต้องรองรับให้ลูกค้าที่มีโลโก้ใช้ได้ → ทำเป็น "มีก็ขึ้น ไม่มีก็เหมือนเดิมเป๊ะ"

ตอนนี้ `logo_url` ใน `app_settings` ต้อง **วาง URL เอง** ยังไม่มีปุ่มอัปโหลด
ถ้าไม่ใส่ → แถบเมนู fallback เป็นตัวอักษรแรกของ `brand_name` ในกล่องสีแบรนด์ (ดูตั้งใจ ไม่ได้ดูขาด)

**งานที่ต้องทำ (ยังไม่เริ่ม)**:
1. สร้าง bucket `branding` ใน Supabase Storage (public read)
2. ปุ่มอัปโหลดในการ์ด "แบรนด์ของกิจการ" (`BrandingCard.tsx`) — ตอนนี้วาง URL อย่างเดียว
3. **โลโก้บนหัวเอกสารการค้า** — ที่ต้องคิดเรื่องการจัดวาง:
   - หัวกระดาษปัจจุบัน (`companyHeader` ใน `print.ts`) เป็น `<table>` 2 ช่อง 50/50 —
     **ซ้าย** = ชื่อไทย/อังกฤษ + ที่อยู่ + เลขภาษี · **ขวา** = ชื่อเอกสาร + ต้นฉบับ/สำเนา
   - แผนที่เสียหายน้อยที่สุด: แทรก `<td>` โลโก้ **ซ้ายสุด** กว้าง ~90px (`img` สูงคงที่ 56px,
     `object-fit:contain`) แล้วลดช่องซ้ายเป็น ~40% — **ช่องขวาไม่ขยับ** เลขเอกสาร/ยอดเงินอยู่ที่เดิม
   - ⚠️ **ไม่มี `logo_url` = ไม่ render `<td>` นั้นเลย** (ไม่ใช่ `<td>` ว่าง) → เอกสารของผู้ใช้เหมือนเดิม 100%
   - ⚠️ ต้องเป็น **URL ที่ public read** — หน้าต่างพิมพ์เป็น `window.open` เขียน HTML ใหม่ ไม่มี session/cookie
     ของแอปติดไปด้วย · โลโก้ที่ต้อง auth จะไม่ขึ้นตอนพิมพ์
   - ⚠️ ต้อง **รอรูปโหลดเสร็จก่อน `w.print()`** — โค้ดตอนนี้รอแค่ `document.fonts.ready` (`openPrint`)
     ถ้าไม่รอ `img.decode()` ด้วย จะได้กระดาษที่โลโก้หายหรือความสูงวัดผิด (มีตรรกะ zoom ย่อให้พอดี A4 อยู่)
   - เทสด้วยกิจการทดสอบ EID99 + โลโก้สมมติ — **ห้ามใส่ logo_url ให้กิจการจริงของผู้ใช้ตอนเทส**

**ผู้ใช้เคยถาม**: ถ้าอยากได้โลโก้/wordmark ให้ Claude ออกแบบให้ได้ — ยังไม่ได้เริ่ม

---

## 4. งาน productization ก้อนใหญ่ — **ฐานเสร็จแล้ว เหลือชั้นบน**

> รายละเอียดการตัดสินใจทั้งหมดอยู่ใน `docs/DECISIONS.md` **D46-D48**

### 4.0 เสร็จแล้ว (migration 0025-0032 · branch `feat/multi-tenant`)

| งาน | สถานะ |
|---|---|
| 4.1 `tenant_id` ทุกตาราง + RLS + เทสกันรั่ว | ✅ 31 ตาราง · 56 policy · index 65 ตัวขึ้นต้นด้วย `tenant_id` |
| 4.2 `entity_id` ผลิต/ขาย/contacts + ผ่าตัด PK | ✅ 16 ตาราง · คีย์ 23 จุด |
| อุด security definer 7 ตัว | ✅ (ไม่ใช่ 25 อย่างที่เคยประเมิน — ที่เหลือเป็น invoker อยู่แล้ว) |
| อุด service-role path | ✅ snapshot/restore · จัดการผู้ใช้ · migration script |
| 4.7 ส่วน auth + แบรนด์ตาม subdomain | ✅ (subdomain กลายเป็นของแต่งหน้าล้วน ๆ หลัง 0032) |
| รหัสผ่าน/ชื่อผู้ใช้ (D48) | ✅ สุ่มรหัสไม่ซ้ำ · บังคับเปลี่ยนครั้งแรก · ชื่อผู้ใช้ห้ามซ้ำทั้งระบบ |

### 4.0.1 ❌ MFA — **ตกไปแล้ว (2026-08-12) อย่าเสนอซ้ำ** · ดู D52

ผู้ใช้ตัดสินว่าไม่ทำ ใช้การเตือนลูกค้าตอนตั้งรหัสผ่านแทน — เหตุผลและ**ความเสี่ยงที่รับไว้**
เขียนครบใน `docs/DECISIONS.md` **D52**

**สิ่งที่ยังเปิดอยู่และควรทำแทน (ถูกกว่ามาก ยังไม่ได้ทำ)**: ขันเกณฑ์ `validatePassword`
(`lib/shared/password.ts`) ให้ปฏิเสธรหัสที่คาดเดาง่าย → บังคับได้จริงโดยไม่ต้องพึ่งวินัยลูกค้า
และไม่มีต้นทุนกับลูกค้าเลย

<details><summary>เหตุผลเดิมที่เคยเขียนว่า "ห้ามข้าม" (เก็บไว้ดูบริบท)</summary>

D48 ปิดทางที่ "เกิดโดยบังเอิญ" ได้แล้ว (ชื่อผู้ใช้ซ้ำ) แต่**ยังเหลือทางที่ตั้งใจเจาะ**:
รู้ชื่อผู้ใช้ของอีกเจ้า + รหัสผ่านบังเอิญตรงกัน → เข้าได้ · ชื่อผู้ใช้ไม่ใช่ความลับ
มีแต่ MFA ที่ปิดได้ — งานที่ต้องทำ: หน้าเปิดใช้ + QR + recovery code + ขั้นตอนกู้ตอนลูกค้าทำมือถือหาย

</details>

### 4.0.1b ✅ LINE ต่อ tenant — **แก้แล้ว (2026-08-12 · migration 0033 · D51)**

`lib/line.ts` เลิกอ่าน env แล้ว — อ่านจาก `app_settings` ต่อ tenant (`line_channel_token` / `line_group_id`)
· tenant มาจาก session เสมอ · **ไม่มี fallback ไป env โดยตั้งใจ** (fallback = ตัวบั๊กเอง)
· โทเคนอ่านได้เฉพาะ `main` — บังคับที่ RLS ไม่ใช่ซ่อน UI (anon key เป็นค่าสาธารณะ)
· UI = บัญชี → ตั้งค่า → การ์ด **"แจ้งเตือน LINE"** (ขึ้นเฉพาะ main · โทเคนแสดงแค่ 4 ตัวท้าย)
· เทส `tests/tenant/line-config.test.ts` 6 ตัว

**เหลือ (ผู้ใช้)**: ย้ายค่าจาก Vercel env เข้าแอป แล้วลบ env ทิ้ง — ขั้นตอนอยู่ใน `docs/GOLIVE_CHECKLIST.md`
⚠️ ระหว่างที่ยังไม่กรอก **แจ้งเตือนจะเงียบ** (ไม่ error ไม่กระทบการบันทึก)

> 🪤 **บทเรียนที่ต้องใช้ต่อ**: เจอตอนตอบคำถามว่า "ต้องทำ Vercel ใหม่ให้ลูกค้าไหม" —
> **env ที่ผูกกับ deployment ทุกตัวต้องไล่ดูว่าควรเป็นค่าต่อ tenant หรือไม่** ไม่ใช่แค่ LINE
> และ**เทส 67 ตัวจับไม่ได้** เพราะดูแต่ข้อมูลใน DB ไม่ได้ดู side effect ที่ยิงออกนอกระบบ
> (ตรวจแล้ว: `DEFAULT_ENTITY_ID`/`LIQUOR_ENTITY_ID` ใน `.env.example` **ไม่มีโค้ดไหนใช้แล้ว** = ซากเก่า ลบได้
> · `ANTHROPIC_API_KEY`/`SCAN_DAILY_LIMIT` = โควตาต่อ deployment ซึ่งตั้งใจอยู่แล้ว แต่ผูกกับฟีเจอร์สแกนใบเสร็จที่จะตัดทิ้ง)

### 4.0.2 ที่ยังไม่ทำในก้อนนี้

~~4.3 (VAT branching)~~ ✅ · ~~4.4~~ ✅ · ~~4.5~~ ✅ — เหลือ:
· 4.6 (โรงแช่ — ยังติดเทมเพลตฟอร์มสรรพสามิต) · ข้อ 3 (โลโก้บนเอกสาร) · ตัดฟีเจอร์สแกนใบเสร็จ

> ✅ **4.3 + 4.4 + 4.5 เสร็จครบแล้ว** (0034 · 0036 · D53 · D55) — เดิม 4.3
> ที่เป็นก้อนใหญ่สุดและเสี่ยงสุด เพราะแตะ `lib/sales/calc.ts` ซึ่งเป็นชั้นสูตร (กติกาเหล็ก)
> `is_vat` ยังไม่มีโค้ดไหนใช้ → **กิจการที่ไม่จด VAT ยังคิด VAT 7% และออกใบกำกับภาษีได้ (ผิดกฎหมาย)**
> `scripts/add-entity.ts` เตือนเรื่องนี้ตอนสร้างกิจการด้วย `--no-vat` แล้ว

### 4.1 สถาปัตยกรรมที่ตัดสินแล้ว — อย่ารื้อ อย่าเปิดประเด็นซ้ำ

| เรื่อง | มติ | เหตุผล (อย่าลืม ไม่งั้นจะถูกเสนอให้เปลี่ยนอีก) |
|---|---|---|
| ที่เก็บข้อมูล | **1 Supabase project + `tenant_id` + RLS** | ดูแลง่าย: migrate/backup/deploy ครั้งเดียว |
| ~~1 project ต่อลูกค้า~~ | ❌ **ยกเลิกมติเดิม** | เปลี่ยนเป็น multi-tenant แล้ว (แผนเก่าเขียนว่าแยก project — ไม่ใช้แล้ว) |
| ความเชื่อมั่นลูกค้า | **สัญญาลายลักษณ์อักษร (NDA/DPA) + เข้ารหัส at-rest + audit log** | พูดได้แค่ *"เปิด DB ตรง ๆ อ่านไม่ได้"* — **ห้ามพูดว่า "เราอ่านไม่ได้"** เพราะกุญแจอยู่ฝั่งเรา = โกหก |
| zero-access จริง | **tier ทางเลือก** — ลูกค้าเปิด Supabase ของตัวเอง | เจ้าที่กังวลสุดค่อยขายตัวนี้ |
| **E2EE (กุญแจฝั่งลูกค้า)** | ❌ **ตกไปแล้ว** | ต้องรื้อ 21 RPC + 34 DB function + 11 trigger + 61 RLS · ทำให้ **กติกาเหล็กพัง** (stock trigger, 1 batch 1 แถว, idempotency) · ลืมรหัส = ข้อมูลหายถาวร |
| codebase | **เดียว ไม่ fork** · กิจการผู้ใช้เอง = tenant #0 | แก้ฟอร์มภาษีครั้งเดียวใช้ได้ทุกเจ้า |
| โฮสต์ | Vercel Pro ทีมเดียว หลาย project | Hobby ห้ามใช้เชิงพาณิชย์ |
| ราคา | ผลิต ฿790 · +บัญชี ฿490 · +ขาย ฿490 · Full ฿1,490 · setup ฿12,000+ · **+กิจการที่ 2 ฿390–590** | |

### 4.2 EID — **ไม่ถอด** แต่ซ่อน + ขายเป็น add-on ที่ผู้ใช้เป็นคนเปิดให้

> ⚠️ **ห้ามลบระบบ EID** — กิจการผู้ใช้เองใช้ 2 entity จริง (EID01 บริษัท จด VAT + EID02 ศิวกร ส่วนตัว ไม่จด VAT
> · bank_accounts 7 บัญชีผูกข้ามทั้งสอง) · SME ไทยมี บริษัท+บุคคลธรรมดา เป็นเรื่องปกติ

- 2 ชั้น: `tenant_id` (แยกลูกค้า, RLS, ผู้ใช้ไม่เห็น) → `entity_id` (กิจการ/โรงงานของลูกค้ารายนั้น)
- `tenants.max_entities` default 1 → UI **ซ่อนตัวเลือกกิจการทั้งหมด** ยัด entity_id ให้อัตโนมัติ
- **บังคับที่ DB ไม่ใช่แค่ซ่อน UI**: RLS ห้ามลูกค้า insert/update ตาราง `entities` — สร้างได้เฉพาะ service role (ผู้ใช้)
  → เป็น add-on ที่ควบคุมได้จริง ลูกค้าเลี่ยงผ่าน API ไม่ได้

### 4.3 ทำให้ผลิต+ขาย แยกตาม entity (ขอบเขตใหม่ ยืนยัน 2026-08-11)

ตอนนี้ **มีแต่ฝั่งบัญชีที่รู้จัก entity** (transactions, tax_summaries, wht_certificates, bank_accounts,
edit_log, profiles, report_runs, snapshots) · ฝั่งผลิตกับขาย **ไม่มี entity_id เลย**

- เติม `entity_id` ฝั่งผลิต + ขาย + `contacts` (~12 ตาราง) → สินค้า/ลูกค้า/สต็อก/batch/เลขเอกสาร **แยกเด็ดขาดต่อกิจการ**
- **ต้องแก้ PK/unique พวกนี้** (ตอนนี้ unique ทั้งระบบ → 2 กิจการใช้ชื่อซ้ำกันไม่ได้):

  | ตาราง | ตอนนี้ | ต้องเป็น |
  |---|---|---|
  | `sale_menu` | `menu_name` unique | unique(entity_id, menu_name) |
  | `warehouse_stock` | PK `item_code` | PK(entity_id, item_code) |
  | `stock_product` | PK `product_id` | PK(entity_id, product_id) |
  | `log_distill` | `batch` unique ← **กติกาเหล็ก** | unique(entity_id, batch) + **แก้ golden test** |
  | `bank_accounts` | `account_name` unique | unique(entity_id, account_name) |

- **เลขเอกสารต่อกิจการ**: ไม่ต้องแก้ schema — `counters` PK เป็น `key` เดี่ยว ฝัง entity ในคีย์ได้เลย (`QU-EID02-260801`)
- ✅ **ผ่าตัด PK/unique เสร็จแล้วใน 0027** (ตารางด้านบนคือของที่ทำไปแล้ว เก็บไว้ดูเหตุผล)

#### 🧨 รายการที่ต้องแก้ตอนทำ 4.3 — ไล่ไว้แล้ว อย่าไล่ซ้ำ (D49, 2026-08-11)

`stock_product` · `warehouse_stock` · `sale_menu` · `log_distill` มีคีย์พ่วง `entity_id` แล้ว
แต่ **โค้ดฝั่งอ่านยังไม่กรอง `entity_id` เลยสักจุด** → พอมีกิจการที่ 2 ที่มีข้อมูลผลิต/ขายจริง
หนึ่งคีย์จะคืน 2 แถว แล้วโค้ดที่ยัดลง `Map` ด้วยคีย์เดิมจะให้แถวหลังทับแถวแรก **เงียบ ๆ ไม่ error**

| ไฟล์ | บรรทัด | ตาราง |
|---|---|---|
| `app/(app)/production/data.ts` | 32 · 55 · 201 · 294 | `log_distill` (เลข batch ถัดไป / ปริมาณคงเหลือ / ประวัติ) |
| `app/(app)/production/data.ts` | 68 | `stock_product` |
| `app/(app)/sales/data.ts` | 44 · 351 | `sale_menu` |
| `app/(app)/sales/data.ts` | 45 · 262 | `stock_product` |
| `app/(app)/sales/data.ts` | 46 · 264 · 367 | `warehouse_stock` |
| `app/(app)/reports/data.ts` | 70 | `log_distill` |

> **วันนี้ยังไม่พัง** เพราะ 0026 backfill ทุกแถวฝั่งผลิต/ขายเป็นกิจการหลักตัวเดียว
> และยังไม่มี UI ให้สร้างข้อมูลผลิต/ขายในกิจการที่ 2 → หนึ่งคีย์ยังคืนแถวเดียวเสมอ
> **ห้ามแก้แบบเดา ๆ ก่อนถึง 4.3** — ต้องรู้ก่อนว่า UI ให้เลือกกิจการยังไง ไม่งั้นรื้อซ้ำ
> · ฝั่ง RPC/trigger กรอง entity ถูกต้องแล้วทั้งหมด (0029) ไม่ต้องแตะ

### 4.4 กิจการที่ไม่จด VAT

- ✅ `entities.is_vat` **มีคอลัมน์อยู่แล้ว** ไม่ต้องเพิ่ม
- ต้องแตกตาม flag: `lib/sales/calc` ข้ามสูตรถอด VAT · ออกได้แค่ **ใบเสร็จรับเงิน/บิลเงินสด** · ไม่เข้า ภพ.30/tax_summaries
- ⚠️ **กฎหมาย**: กิจการไม่จด VAT **ออกใบกำกับภาษีไม่ได้ (ผิดกฎหมาย)** → ต้อง **บล็อกแข็งที่ DB/RPC** ไม่ใช่แค่ซ่อนปุ่ม

### 4.5 ประเภทโรงงาน กลั่น vs แช่

> ✏️ **แก้จากแผนเดิม**: `factory_type` ต้องอยู่ที่ **`entity`** ไม่ใช่ tenant —
> ลูกค้ารายเดียวมี 2 โรง (กลั่น 1 + แช่ 1) ได้ · `entities.excise_id` มีอยู่แล้ว = เลขสรรพสามิตแยกต่อโรงได้ทันที

- กลั่น (ปัจจุบัน): หมัก→กลั่น→ปรุง→บรรจุ + ฟอร์ม ภส. ชุดกลั่น
- แช่ (เบียร์/ไวน์/สาโท): หมัก→บ่ม→บรรจุ **ไม่มีขั้นกลั่น** + ฟอร์ม ภส. คนละชุด
- ⚠️ **บล็อกอยู่**: ยังไม่มีเทมเพลตฟอร์มสุราแช่จากสรรพสามิต — ผู้ใช้มีแต่ฟอร์มกลั่น (ต้องไปขอมาก่อน)
- ⚠️ **ห้ามเปิดโหมดแช่ในระบบที่ผู้ใช้ใช้จริง** (เขาไม่ได้ทำแช่) — เป็น config เท่านั้น

### 4.6 ที่เหลือ

- **Module feature flags** — `modules_enabled` ต่อ tenant (ผลิต/บัญชี/ขาย = 7 SKU) gate ที่ nav + route/server-action + จุดเชื่อมข้ามโมดูล
- **Provision script** สร้าง tenant ใหม่ + secrets registry + fleet cron (keep-alive + backup)
- **ตัด/ลดฟีเจอร์สแกนใบเสร็จ AI** — memory `receipt-scan-thai-unreliable` (อ่านสลิปไทยไม่แม่นพอ ขายแล้วลูกค้าผิดหวัง)
- ⚠️ **จุดเสี่ยงอันดับ 1 ของ multi-tenant**: RLS พลาด = ลูกค้าเห็นข้อมูลกัน → ต้องมี **เทสอัตโนมัติต่อ policy** ก่อนรับลูกค้าจริง

### 4.7 ลูกค้าเข้าลิงก์ไหน — URL ไม่ใช่ตัวกำหนดสิทธิ์

**ลิงก์เดียวกันทุกคนได้** — ตัวกำหนดว่าเห็นข้อมูลใครคือ **บัญชีที่ล็อกอิน**:
`auth.uid()` → `profiles.tenant_id` → `my_tenant()` → RLS กรองทุกตาราง
(ต่อยอด pattern เดิมตรง ๆ: มี `my_role()` / `my_entities()` ใน `20260720000006_rls.sql` อยู่แล้ว)

| แบบ | URL | deploy | โชว์แบรนด์ลูกค้าที่หน้า login ได้ไหม |
|---|---|---|---|
| A. ลิงก์เดียว | `app.แบรนด์เรา.com` | 1 | ❌ (ยังไม่รู้ว่าใคร) |
| **B. subdomain ต่อลูกค้า** | `โรงก.แบรนด์เรา.com` | 1 (wildcard) | ✅ |
| C. โดเมนลูกค้าเอง | `erp.แบรนด์ลูกค้า.com` | 1 + ตั้งโดเมนต่อราย | ✅ |

> 🔗 **เชื่อมกับข้อ 2 (white-label ที่ยังค้าง)**: หน้า login อ่านแบรนด์จาก DB ไม่ได้เพราะ RLS บล็อกก่อน login
> → **แบบ B แก้ปัญหานี้พอดี** (รู้ tenant จาก subdomain ตั้งแต่ก่อนล็อกอิน) · ถ้าจะไป white-label เต็ม แบบ B แทบเป็นตัวบังคับ

> 🚨 **ห้ามใช้ subdomain/URL เป็นตัวตัดสินสิทธิ์เข้าถึงข้อมูล** — ใช้ได้แค่แต่งหน้า/ชี้ทาง
> ถ้าเขียนว่า "อยู่ subdomain ไหน = เห็นข้อมูล tenant นั้น" → ใครก็พิมพ์ URL คนอื่นแล้วเห็นข้อมูลได้
> **สิทธิ์ต้องมาจาก `profiles.tenant_id` + RLS เท่านั้น**

**ข้อมูลกิจการผู้ใช้เองควรอยู่ DB ไหน?** — แผนเดิมว่า tenant #0 ในระบบรวม แต่ควรพิจารณาแยก:
ลูกค้าคือคู่แข่งของผู้ใช้ → RLS พลาดทีเดียวรั่ว **สองทาง** · ทางที่ปลอดภัยกว่าคือให้กิจการผู้ใช้อยู่
**Supabase project ของตัวเอง แต่รันโค้ดชุดเดียวกัน** (env ชี้คนละ DB) — ยังได้ dogfooding เต็ม
และเป็น **กลไกเดียวกับ tier "ลูกค้าเป็นเจ้าของ DB"** ที่จะขายอยู่แล้ว = ไม่มีงานเพิ่ม

### 4.8 ⚡ กันระบบช้า — **ห้ามลืม อ่านก่อนเขียน migration/query ทุกครั้ง**

> ตรวจแล้ว: **ขนาดข้อมูลไม่ใช่ปัญหา** — export จริงของผู้ใช้ (หลายปี) รวม ~1.2 MB
> → ใน Postgres+index ≈ **~5 MB/ลูกค้า/3 ปี** (~2 MB/ลูกค้า/ปี)
> 8 GB (Pro) รองรับ **50 ลูกค้าได้ ~80 ปี** · 200 ลูกค้าได้ ~20 ปี → **อย่าเสียเวลากังวลเรื่องพื้นที่**

**ตัวที่ทำให้ช้าจริงคือพวกนี้ ไม่ใช่ขนาด:**

| ความเสี่ยง | กติกาที่ต้องทำ |
|---|---|
| 🔴 **ไม่มี index บน `tenant_id`** ← อันตรายสุด | **ทุก index ต้องขึ้นต้นด้วย `tenant_id`** (composite เช่น `(tenant_id, transaction_date)`, `(tenant_id, entity_id, batch)`) · ลืม = ทุก query สแกนข้อมูลลูกค้าทุกเจ้า และจะไม่รู้ตัวจนลูกค้าเยอะ |
| RLS policy แพง | helper ต้องเป็น `stable security definer` แบบเดิม (`my_role()`/`my_entities()` เขียนถูกแล้ว) — ประเมินครั้งเดียวต่อ query ไม่ใช่ต่อแถว · **อย่าเขียน subquery ตรง ๆ ใน policy** |
| connection เต็มตอนหลายเจ้าใช้พร้อมกัน | เปิด **connection pooler** ของ Supabase (Micro ~60 connections) |
| CPU/RAM ไม่พอ | อัปไซซ์ instance — จ่ายเงินแก้ได้ ไม่ต้องแก้โค้ด |

**เริ่มมองเมื่อไหร่:** < 20 ลูกค้า = ไม่ต้องคิด · 20–100 = ดู pooler + ตรวจ index · 100+ = ค่อยคิด partition

**แผนสำรองถ้าโตจริง (ไล่จากถูก→แพง):**
```
1. อัปไซซ์ compute            ← จ่ายเงิน แก้ทันที ไม่แตะโค้ด
2. ตรวจ/เพิ่ม index            ← 90% ของอาการช้าจบตรงนี้
3. ย้ายข้อมูลเก่าไปตารางเก็บถาวร (กฎหมายเก็บ 5-7 ปี ลบไม่ได้ แต่ย้ายออกจากตารางร้อนได้)
4. partition ตาราง transactions/log_* ด้วย tenant_id
5. ย้ายลูกค้ารายใหญ่ออกไป project ของตัวเอง   ← = tier แยก DB ที่ขายอยู่แล้ว
```
> 💡 ข้อ 5 คือ **สินค้า tier พรีเมียมกับแผนสำรองเป็นของชิ้นเดียวกัน** → ยิ่งตอกย้ำว่า provision/fleet script ต้องทำตั้งแต่แรก

### 4.9 เซ็ตอัพ tier "แยก DB" ยุ่งแค่ไหน (ไว้ตั้งราคา)

**งานเทคนิคล้วน ๆ ≈ 45 นาที (ทำมือ) / ~10 นาที (มีสคริปต์)**: สร้าง project + เชิญเราเป็น member →
`db:push` 24 migration → seed ค่าตั้งต้น → สร้าง user/role → Vercel env+โดเมน
**ตัวกินเวลาจริงคือย้ายข้อมูลเดิม (ชม.–วัน) + เทรน (2-4 ชม.) ซึ่งเท่ากันทั้งสอง tier**

⚠️ **ต้นทุนที่ซ่อนอยู่สำคัญกว่าตอนติดตั้ง**: ออก version ใหม่ที่มี migration = ต้องรัน **ทุก DB**
(มี fleet script = ไม่กี่นาที · ไม่มี = นรกตอนลูกค้า 10 ราย) + backup ต้องวน + support ต้องรู้ว่าเจ้าไหนอยู่ DB ไหน

→ **ตั้งราคา tier แยก DB บวก +฿500–990/เดือน** (หรือค่าติดตั้ง +฿5,000) เพราะแบกงานต่อ release ตลอดอายุลูกค้า
· ถ้าลูกค้าเปิด Supabase **Free ของตัวเอง** ค่า infra ที่เราแบก = ฿0 → ส่วนต่างเป็นค่าดูแลล้วน = มาร์จิ้นดี

---

## 5. งานเล็กที่ยังค้าง

### จาก `docs/APP_REVIEW_2026-07.md` (ปิดครบแล้ว เหลือ polish)
- touch target บางจุดยัง < 44px (ที่แก้แล้ว: ปุ่มในลิสต์/ตาราง/ออเดอร์/ค้นบิล)
- `input type="month"` picker เล็กบนมือถือ (มีปุ่ม ‹ › ช่วยแล้ว)

### จาก D43
- อิโมจิเหลือ 5 จุด **ตั้งใจเก็บ** — `↔` ในคอมเมนต์โค้ด 4 + `⚠️` ใน `app/global-error.tsx`
  (หน้าหลังใช้ inline style เพราะ CSS อาจโหลดไม่สำเร็จ จึงใช้ SVG ไม่ได้)

### ค้างจาก Phase 5 (migration) — ยังไม่ได้ทำ
- **เทียบมือ**: ยอดบัญชีในแอปใหม่ vs ระบบเดิม ณ วันเดียวกัน
- **เทียบมือ**: PDF ภพ.30 + ภส. เดือนล่าสุดที่ยื่นจริง วางทับกับที่ยื่นไป

### Phase 6 (ยังไม่เริ่ม)
UAT + shadow verification + cutover kit — ดู `docs/MIGRATION_PLAN.md` sec 12

---

## 6. ค่าใน production — ✅ **ครบแล้ว (ผู้ใช้ยืนยัน 2026-08-02)**

- [x] `entities.excise_id` (EID01) — กรอกเลขสรรพสามิต 17 หลักแล้ว
- [x] `app_settings` `sales_revenue_entity` / `sales_revenue_account` — ตั้งแล้ว
- [~] `bank_accounts.opening_balance` = 0 ทุกบัญชี — **จงใจไม่ใส่ ไม่ใช่งานค้าง**
      เพราะใน `transactions` มีรายการนำเงินเข้าบัญชีตั้งแต่ต้นแล้ว → ใส่ยอดยกมาอีก = **นับซ้ำ**
      ⚠️ ห้ามเสนอให้เติมทีหลังเพราะเห็นเป็น 0
- [x] `ANTHROPIC_API_KEY` — ตั้งแล้ว
- [x] `LINE_CHANNEL_TOKEN` / `LINE_GROUP_ID` — ตั้งแล้ว
- [x] **GitHub auto-deploy เชื่อมแล้ว** — push `main` = deploy production เอง (เลิกใช้ `vercel --prod`)

---

## 7. บทเรียนจาก D43 (อ่านก่อนแก้ UI/เขียนสคริปต์)

1. **selector trap**: `data-mode` อยู่บน `<html>` แต่ `data-brand` อยู่บน div ใน `(app)/layout.tsx`
   → `[data-mode="dark"][data-brand="x"]` **ไม่ match** ต้องเขียนคู่กับแบบลูกหลาน
   **build ผ่านทั้งที่สีผิด** — จับได้ตอนอ่าน computed style ในเบราว์เซอร์เท่านั้น
2. **สคริปต์ sweep**: ห้าม `replace()` แบบ global ที่แตะ `"` — รอบแรกไปกิน string literal ทั้งไฟล์
   (`from "react"` → `from"react"`) ต้อง revert · แก้เฉพาะ "ภายในค่าของ className"
3. **หลัง sweep สีอัตโนมัติ ต้องไล่หาคู่ `bg-x` + `text-x` ที่เป็น token เดียวกัน**
   (เจอปุ่มสแกนใบเสร็จกลายเป็น `bg-brand text-brand` = ตัวหนังสือหายไปกับพื้น)
4. **ไฟล์เอกสารพิมพ์ห้ามผูกกับธีม**: `print.ts` · `reportHtml.ts` · `lib/pdf/*`
   ต้องดำบนขาวเสมอ ไม่งั้นเปิดโหมดมืดแล้วพิมพ์ = กระดาษพื้นดำ
5. **ตรวจสีดิบเหลือ 0**:
   ```bash
   grep -rn "\(bg\|text\|border\)-\(slate\|amber\|green\|red\|blue\)-[0-9]" app --include=*.tsx | grep -v print.ts
   ```

---

## 8. ลำดับที่แนะนำ (อัปเดต 2026-08-11)

```
✅ เสร็จแล้ว: D45 มัดจำ · ข้อ 2 co-brand · 4.1 + 4.2 + auth ของ 4.7 (branch feat/multi-tenant)

✅ 2026-08-12: ตรวจโค้ดหลังผ่าตัด PK (D49) · ผู้ใช้เทส demo tenant ในเบราว์เซอร์ครบ (ข้อ 9)

ถัดไป — เรียงตามที่ควรทำ:
1. ~~ผู้ใช้เทส demo tenant ในเบราว์เซอร์~~                  ← **ผ่านแล้ว 2026-08-12**
2. ขั้น 6 — ย้าย DB production ตามมา (ข้อ 4.10)            ← **คิวถัดไป** · เสี่ยงสุด ต้องยืนยันทีละขั้น
3. เทียบมือยอดบัญชี + PDF ภพ.30/ภส. (ค้างจาก Phase 5)     ← ผู้ใช้ทำเอง ทำขนานได้
4. ~~4.4 max_entities + ซ่อน UI เลือกกิจการ~~ **เสร็จแล้ว (D53)**
5. ~~4.3 VAT branching~~ **เสร็จแล้ว (D55 · 0036)** — กิจการไม่จด VAT ใช้ได้จริงแล้ว
6. ~~4.5 module flags → provision script~~ **เสร็จแล้ว (D53)** — `npm run provision:tenant` / `provision:add-entity`
7. ~~MFA~~ **ตกไปแล้ว (D52)** · ~~LINE ต่อ tenant~~ **เสร็จแล้ว (D51)** → แทนที่ด้วย: ขันเกณฑ์รหัสผ่าน `validatePassword`
8. โลโก้บนหัวเอกสาร (ข้อ 3) · 4.6 โรงแช่ (รอเทมเพลตฟอร์ม)
```

### 4.10 ✅ ขั้น 6 — ย้าย DB production **เสร็จแล้ว (2026-08-12)** เหลือแค่ merge+push

**DB production อยู่ที่ migration 0032 แล้ว** · ข้อมูล 1,685 แถว 30 ตาราง ตรงกับไฟล์สำรองเป๊ะ
· EID01+EID02 ครบ · ผู้ใช้ `ceo` ล็อกอินเดิมได้ · ผู้ใช้ตรวจในเบราว์เซอร์ผ่านครบแล้ว

| ทำอะไรไป | ผล |
|---|---|
| 1. สำรองข้อมูล | `scripts/backup-tables.ts` → `D:\insep-erp-backup\2026-08-12-before-0025\` (1,685 แถว) |
| 2. เชิญบัญชีใหม่เข้า org เก่า → `supabase link` | CLI เห็นทั้ง 2 project · link ไป `vmhiwlxdyhatucioalzp` |
| 3. `db push` รอบแรก | ❌ **ล้มที่ 0026** (D50 — trigger audit) · 0026 ย้อนกลับสะอาด ข้อมูลไม่เสีย |
| 4. แก้ 0025+0026 ปิด user trigger ตอน backfill → push ใหม่ | ✅ ครบ 0026-0032 |
| 5. ลบ audit ปลอม 674 แถวที่ 0025 รอบแรกทิ้งไว้ | ✅ พิสูจน์ก่อนลบ 4 ด่าน · เหลือของจริง 43 แถวตรงไฟล์สำรอง |

**เหลือขั้นเดียว**: merge `feat/multi-tenant` → `main` → push (= deploy production)
⚠️ ตอนนี้ **DB ใหม่ + โค้ดที่ deploy อยู่ยังเก่า** — ใช้งานได้ปกติเพราะโค้ดเก่าไม่รู้จักคอลัมน์ใหม่
แต่ก็ไม่ได้ใช้มัน · อย่าทิ้งไว้นาน

> 🪤 **บทเรียนที่ต้องใช้กับลูกค้ารายต่อไป**: migration ที่ backfill ข้อมูลเดิม **ต้องปิด user trigger**
> ไม่งั้นล้ม (หรือทิ้งขยะใน audit log) · DB ทดสอบที่ยังไม่มีข้อมูลจับไม่ได้ — ดู D50

---

## 9. ✅ ผู้ใช้เทสใน browser ครบแล้ว (2026-08-12) — **ผ่านทั้งหมด**

ครบทุกข้อในตารางล่าง รวมข้อ 9 (เข้าทุกแท็บของทั้ง 3 โดเมน — ไม่มีหน้าไหน error)
→ ยืนยันผลตรวจโค้ด D49 ด้วยของจริงอีกชั้น · **พร้อมไปขั้น 6 (ย้าย DB production) แล้ว**

> 🪤 **กับดักที่เสียเวลาไปจริงตอนเทส — อ่านก่อนเทสหน้า login รอบหน้า**
> ผู้ใช้เปิด `localhost:3000/login` (ไม่มี subdomain) แล้วรายงานว่า "สีแบรนด์ไม่เปลี่ยนตามอะไรเลย"
> ทั้งที่ระบบทำงานถูก — หน้าไม่มี subdomain **ถูกออกแบบให้คงที่เสมอ**
> **subdomain ไม่เกี่ยวกับการล็อกอิน** (ผลของ D48) → กรอกชื่อผู้ใช้ที่ URL ไหนก็เข้าได้
> และหลังเข้าแอปแล้วสีมาจาก tenant ที่ล็อกอินอยู่ ไม่ได้มาจาก URL
> → **ข้อ 1/2/6 เท่านั้นที่ต้องเปิดด้วย `<slug>.localhost:3000` จริง ๆ** ข้ออื่นเปิด URL ไหนก็ได้
> วิธีดูว่าอยู่หน้าไหนโดยไม่ต้องดู URL: มีคำโปรย "ระบบภายในโรงกลั่นสุราคราฟต์" = หน้าเริ่มต้น ·
> มี `powered by` + แถบสีบนการ์ด = หน้าของลูกค้า

Claude ล็อกอินเองไม่ได้ (ห้ามกรอกรหัสผ่าน) → ส่วนหลัง login ต้องให้ผู้ใช้ดู
ที่ตรวจอัตโนมัติผ่านหมดแล้วคือ RLS/RPC/คีย์ (67 เทส) และหน้า login (ตรวจในเบราว์เซอร์แล้ว)

**เตรียม**: `.env.local` ชี้ project ใหม่อยู่แล้ว · มี tenant สาธิต 2 เจ้า
ผู้ใช้: `owner-rongkor` / `owner-rongkhor` (ชื่อผู้ใช้ = `owner-<slug>` เสมอ — `tests/tenant/harness.ts:169`)
**รหัสชั่วคราวถูกเปลี่ยนไปแล้วตอนล็อกอินครั้งแรก → ใช้รหัสที่ตั้งใหม่**
(สร้าง tenant เพิ่มได้ด้วย `npm run seed:demo-tenant -- --slug=xxx --name="ชื่อ" --color=copper`
· ⚠️ สคริปต์ไม่ยอม seed ทับ slug เดิม และ**ไม่มีทางสั่งพิมพ์รหัสชั่วคราวซ้ำ** — ถ้าลืมรหัสต้องใช้ slug ใหม่)

```bash
npm run dev
```

| # | ทดสอบ | ควรเห็น |
|---|---|---|
| 1 | เปิด `rongkor.localhost:3000/login` เทียบ `rongkhor.localhost:3000/login` | ชื่อ+สีปุ่มต่างกัน · มี `powered by` |
| 2 | เปิด `localhost:3000/login` (ไม่มี subdomain) | "Insep ERP" + คำโปรยเดิม · **ไม่มี** powered by |
| 5 | เข้าแอปแล้วดูสีธีม | **ตรงกับสีบนหน้า login** (ถ้าเป็นสีเริ่มต้น = D47 หลุดกลับมา) |
| 6 | เปลี่ยนสีในแท็บตั้งค่า → ออกมาหน้า login | เปลี่ยนตาม |
| 7 | หน้าลงหมัก | เสนอ batch `10/69` (ต่อจาก 9/69 ของตัวเอง ไม่ใช่ของอีกเจ้า) |
| 8 | ตั้งค่า → ผู้ใช้ → สร้างผู้ใช้ชื่อ `owner-rongkhor` (ชื่อของอีกเจ้า) | ถูกปฏิเสธ + บอกให้เติมชื่อโรงต่อท้าย |
| 3+4 | สร้างผู้ใช้ใหม่ → ลองตั้งรหัส `12345678` → ตั้งรหัสที่ผ่านเกณฑ์ → logout → login ด้วยผู้ใช้ใหม่ | `12345678` ถูกปฏิเสธตั้งแต่ตอนสร้าง · ผู้ใช้ใหม่เด้งไปหน้า "ตั้งรหัสผ่านใหม่" ก่อนเข้าแอปได้ |
| 9 | เข้าทุกแท็บของทั้ง 3 โดเมน (ผลิต/บัญชี/ขาย) | ไม่มีหน้าไหน error — ทุกตารางมี `tenant_id`/`entity_id` ใหม่ |

> ข้อ 3+4 ทำซ้ำได้เสมอโดยไม่ต้อง seed ใหม่ — `createUserAction` ไม่ส่ง `skip_password_change`
> → trigger `handle_new_user` (0031) ตั้ง `must_change_password = true` ให้ผู้ใช้ใหม่ทุกคนอัตโนมัติ

> ข้อ 9 สำคัญที่สุดในลิสต์ — เป็นตัวเดียวที่จะจับได้ว่ามี query ไหนพังจากการเปลี่ยน PK
> (ตรวจฝั่งโค้ดไปแล้วไม่พบจุดพัง — D49 · ข้อ 9 คือการยืนยันด้วยของจริงอีกชั้น)

---

## 10. แอปจัดการหลังบ้าน (platform admin) — ✅ **เฟส 1 เสร็จ (2026-08-13 · 0035 · D54)**

route `/platform` · requirement เต็ม `docs/ADMIN_APP_REQUIREMENTS.md` · เหตุผลการออกแบบ `docs/DECISIONS.md` D54

| ทำอะไรได้แล้ว | หมายเหตุ |
|---|---|
| รายชื่อลูกค้า (โมดูล · กิจการ x/โควตา · จำนวนผู้ใช้ · วันเปิดใช้) | กรองแถว `is_platform` ของแอดมินเองออก |
| รับลูกค้าใหม่ | ได้ระบบเปล่า · รหัสชั่วคราวขึ้นจอครั้งเดียว + ปุ่มก๊อป |
| เปิด/ปิดโมดูล · ขยายโควตา · เพิ่มกิจการ | ขยายโควตากับเพิ่มกิจการเป็นคนละปุ่ม (D53) |
| **รีเซ็ตรหัสผู้ใช้ของลูกค้า** | งานที่เมื่อก่อน "ยังไม่มีวิธีที่ทดสอบแล้ว" |

**ไฟล์หลัก**: `lib/platform/{guard,auth,provision}.ts` · `app/(platform)/` · `middleware.ts`
· `scripts/grant-platform-admin.ts` · เทส `lib/platform/*.test.ts` + `tests/tenant/platform-tables.test.ts`

### ผู้ใช้ต้องทำเอง (ยังไม่ได้ทำ)
1. `npm run db:push` (0035) — **DB แพลตฟอร์มลูกค้า** (`tnuxrufpzeyuvwdmkojv`) และ DB จริงถ้าจะใช้ที่นั่นด้วย
2. `npm run platform:grant-admin -- --env=.env.local --username=... --create` (ครั้งเดียว)
3. ตั้ง `PLATFORM_ADMIN=1` ที่ **Vercel project ของแอดมินเท่านั้น**
4. `npm run test:tenant` — ต้องผ่านหลังข้อ 1 (ก่อนหน้านั้นตารางยังไม่มี เทสจะฟ้อง)
· ขั้นตอนละเอียดอยู่ใน `docs/GOLIVE_CHECKLIST.md` หัวข้อ "ตั้งแอปจัดการหลังบ้าน"
· ชุดเทสในเบราว์เซอร์อยู่ `docs/TESTING.md` ส่วนที่ 29

### 10.1 โครงสร้าง deployment — **3 Vercel project จาก repo เดียว branch เดียว** (ตอบคำถามผู้ใช้ 2026-08-13)

> สถานะตอนถาม: มี project เดียว `insep-erp` ชี้ **DB ของเจ้าของ** · **ยังไม่มี deployment ไหนชี้ DB ลูกค้าเลย**
> (= ลูกค้ายังเข้าใช้ไม่ได้อยู่แล้ว ไม่ใช่แค่แอปแอดมิน)

| project | ชี้ DB | `PLATFORM_ADMIN` | ใครเข้า |
|---|---|---|---|
| `insep-erp` (มีแล้ว) | `vmhiwlxdyhatucioalzp` | ไม่ตั้ง | เจ้าของ ใช้ทำงานจริง |
| `proof-app` (ยังไม่สร้าง) | `tnuxrufpzeyuvwdmkojv` | ไม่ตั้ง → `/platform` = **404** | ลูกค้าทุกเจ้า |
| `proof-admin` (ยังไม่สร้าง) | `tnuxrufpzeyuvwdmkojv` | **`=1`** | เจ้าของระบบคนเดียว |

> ชื่อสินค้า = **PROOF** (เคาะ 2026-08-17 · D56) — ชื่อ project กลายเป็น URL ฟรี `<ชื่อ>.vercel.app`
> **ยังไม่ต้องซื้อโดเมน**: ไม่ตั้ง `NEXT_PUBLIC_ROOT_DOMAIN` = โหมดลิงก์เดียว ทุก tenant ใช้ URL เดียวกัน
> (subdomain เป็นของแต่งหน้าล้วน ๆ ตั้งแต่ D48 — ไม่มีก็ล็อกอินได้ปกติ)

**env ที่ต้องตั้งบน `proof-app`** (ก๊อปค่าจาก `.env.local.testing-backup`):
| ตัวแปร | จำเป็น | เหตุผล |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ | แอปเข้า DB ไม่ได้เลยถ้าไม่มี |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ | จัดการผู้ใช้ · LINE · snapshot/restore พังทั้งหมดถ้าไม่มี |
| `ANTHROPIC_API_KEY` · `SCAN_DAILY_LIMIT` | ไม่ | มีแค่ฟีเจอร์สแกนใบเสร็จ (ตั้งใจจะเลิกทำ) · ไม่ตั้ง = ปุ่มขึ้นข้อความบอกเอง ไม่พัง |
| `PLATFORM_ADMIN` | ❌ **ห้ามตั้ง** | ตั้งเมื่อไหร่ ลูกค้าเปิด `/platform` เจอหน้า login แทน 404 = รู้ว่ามีระบบคุมลูกค้า |
| `LOGIN_EMAIL_DOMAIN` | ❌ **ห้ามตั้ง** | เปลี่ยน = บัญชีที่สร้างไว้แล้วล็อกอินไม่ได้ทั้งหมด (D56) |
| `NEXT_PUBLIC_ROOT_DOMAIN` | ❌ ยังไม่ตั้ง | ตั้งเมื่อซื้อโดเมน + ผูก wildcard แล้วเท่านั้น |

- **แอดมินต้องแยก project แม้ชี้ DB เดียวกับ `insep-app`** — ตั้ง flag บน project ลูกค้าเมื่อไหร่
  ลูกค้าเปิด `/platform` จะเจอหน้า login แทน 404 = รู้ว่ามีระบบคุมลูกค้าอยู่
- **เปิด Vercel Deployment Protection (password) ที่ project แอดมิน** — ล็อกชั้นนอกที่ไม่กระทบลูกค้า
  และเป็นตัวชดเชยที่ตรงจุดที่สุดของการตัด MFA ทิ้ง (D52)
- ค่า env ของ project ลูกค้า/แอดมิน เอาจาก `.env.local.testing-backup` (ชี้ `tnuxr…` อยู่แล้ว)
- เข้าใช้จริง: เปิด URL ของ project แอดมิน → หน้า login → ล็อกอิน → **ระบบพาไป `/platform` เอง**
  (`admin` เป็น subdomain สงวน → ผูก `admin.<โดเมน>` ได้โดยไม่ชน slug ลูกค้า)

### 10.2 ลูกค้าที่ขอ **แยก DB** จะจัดการยังไง

**ข้อเท็จจริงของโค้ดวันนี้**: `createAdminClient()` อ่าน env ตัวเดียว → **1 deployment = 1 DB**
แอปแอดมินเห็นเฉพาะ DB ที่ env ชี้ · ลูกค้าที่อยู่ DB อื่นมองไม่เห็นเลย (ตั้งใจให้เรียบง่าย ไม่ใช่บั๊ก)

| แบบ | ใครถือ service role key | จัดการจากหน้าจอได้ไหม |
|---|---|---|
| A. เราเปิด Supabase project ให้ แค่แยกก้อน | เรา | ได้ |
| B. **ลูกค้าเปิด Supabase ของตัวเอง** (tier zero-access · ข้อ 4.9) | **ลูกค้า** | ❌ ไม่ได้ เว้นแต่เขาส่งกุญแจให้ |

> 🚨 **แบบ B ห้ามเปิด `PLATFORM_ADMIN` บน deployment ของลูกค้าเด็ดขาด** — ลูกค้าเข้า Dashboard
> ของตัวเองได้ → เพิ่มตัวเองเข้า `platform_admins` ได้ = หน้าจอที่ออกแบบมาคุมลูกค้าไปอยู่ในมือเขา

**ทางที่เลือกไว้ (ลูกค้ากลุ่มนี้ยัง = 0 ราย): ยังไม่แก้โค้ดอะไร** พอมีรายแรกให้ทำแบบ **1 DB = 1 คู่ deployment**
· แบบ B ใช้สคริปต์แทนหน้าจอ (`--env` รองรับอยู่แล้ว — ทำไฟล์ `.env.<ลูกค้า>` เก็บไว้ในเครื่อง)

**ค่อยทำ fleet (หน้าเดียวคุมทุก DB) เมื่อ** ลูกค้าแยก DB ถึง ~3 ราย:
- เก็บ URL+key ต่อ DB เป็น env ของ project แอดมิน + dropdown เลือก DB
- **จุดที่ต้องแก้มีจุดเดียวคือ "ที่มาของ client"** — ทุกฟังก์ชันใน `lib/platform/provision.ts`
  ออกแบบให้ **รับ client เข้ามา** ไว้แล้วตั้งแต่แรก จึงไม่ต้องรื้อ
- 🚨 deployment เดียวถือกุญแจแม่ของทุก DB = เป้าหมายที่คุ้มค่าที่สุดในระบบ
- ⚠️ **ตัวกินเวลาจริงของ tier นี้ไม่ใช่หน้าจอ** แต่คือ migration + backup ต้องวนทุก DB ทุกเวอร์ชัน
  → ถ้าจะลงแรง ลงที่ **สคริปต์ fleet ที่วนไฟล์ env** ก่อน คุ้มกว่าทำ UI

### 🪤 กับดักที่ต้องรู้ก่อนแตะงานนี้ต่อ
- **ตารางใหม่ทุกตัวของแพลตฟอร์มต้อง `enable rls` (ไม่มี policy) + `revoke all from anon, authenticated`**
  Supabase grant สิทธิ์ให้ตารางใหม่อัตโนมัติ · ลืม = ราคาที่ลูกค้าแต่ละเจ้าจ่าย รั่วให้เจ้าอื่นเห็น
- **`lib/platform/provision.ts` ห้าม `import "server-only"`** — สคริปต์เรียกไฟล์นี้ด้วย (จะ throw บน node)
- **`tenants.is_active` ยังไม่บล็อกอะไรเลย** — ปิดแล้วลูกค้ายังล็อกอินได้ปกติ
  → จงใจยังไม่ใส่ปุ่มระงับ (ปุ่มที่กดแล้วไม่เกิดอะไร อันตรายกว่าไม่มีปุ่ม) · ทำคู่กับเฟส 2

### เฟส 2/3 ที่ยังไม่เริ่ม
ตาราง `subscriptions` + `subscription_payments` (ค่างวด) → เตือนอัตโนมัติทางอีเมล
· **ต้องตอบคำถามข้อ 3 ใน requirement ก่อน**: เก็บเงินตัดรอบตามวันเริ่มของแต่ละราย หรือพร้อมกันสิ้นเดือน
