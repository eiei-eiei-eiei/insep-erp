# Insep ERP (Next.js + Supabase) — คู่มือประจำ repo

> ระบบ ERP ภายในของโรงกลั่นสุราคราฟต์ (solo entrepreneur) — ย้ายมาจาก Google Apps Script + Google Sheets 3 แอป
> **เอกสารหลัก**: `docs/MIGRATION_PLAN.md` (แผนละเอียดทุก section — อ่านก่อนทำงานทุกครั้ง) + `docs/FLOW_REDESIGN.md` (โฟลว์/UI ใหม่ 4 workspace — ถ้าขัดกัน: เรื่องโฟลว์/UI ยึด FLOW_REDESIGN, เรื่องสูตร/ข้อมูล ยึด MIGRATION_PLAN sec 6)
> **งานที่เหลือ/ส่งต่อ session ใหม่**: `docs/NEXT_STEPS.md` — **อ่านก่อนเริ่มงานใหม่ทุกครั้ง**
> **แอปจัดการหลังบ้าน (เฟส 1 เสร็จ · route `/platform`)**: `docs/ADMIN_APP_REQUIREMENTS.md`
> **หน้าตา/สี/ไอคอน**: `docs/DESIGN_SYSTEM.md` — **อ่านก่อนแตะ UI ทุกครั้ง** · ห้ามเขียนคลาสสีดิบ (`bg-slate-800`, `text-red-500`, hex) ใน component ใช้ token เท่านั้น
> **โค้ดระบบเดิม (reference)**: `docs/legacy/production/`, `docs/legacy/accounting/`, `docs/legacy/sales/`
> **ผู้ใช้เขียนโค้ดไม่ได้** — ส่งมอบไฟล์เต็มเสมอ อธิบายขั้นตอนที่ผู้ใช้ต้องทำเอง (รันคำสั่ง/กดปุ่ม) ทีละบรรทัด ตอบภาษาไทย คงศัพท์เทคนิคอังกฤษ

## สถาปัตยกรรม (ตัดสินใจแล้ว — อย่ารื้อ)

- **1 Next.js app (App Router, TypeScript, Tailwind), 1 Vercel project, 1 Supabase project (schema `public`)**
- 3 โดเมนแยกด้วย route: `/production` (โรงกลั่น) · `/accounting` (บัญชี multi-entity) · `/sales` (ขาย B2B + คลัง)
- Pattern: client component (UI) + **Server Actions** ใน `actions.ts` ต่อโดเมน — แทน `google.script.run` เดิม
- **ไม่มี webhook ระหว่างแอปแล้ว** — integration ทุกจุดคือ DB transaction / Postgres RPC ใน Supabase เดียวกัน · idempotency ด้วย unique index (`transactions.idempotency_key`, `integration_log(action, idempotency_key)`)
- Auth: Supabase Auth + ตาราง `profiles` (role: `main`/`viewer`/`sale`/`warehouse`, `allowed_entity_ids`) — **สิทธิ์ enforce ด้วย RLS ฝั่ง DB** ไม่ใช่แค่ UI
- PDF ราชการ: **client-side pdf-lib** (npm ไม่ใช่ CDN) — template/font ใน Supabase Storage bucket `pdf-templates` · 3 กลไก: coordinate overlay (ภส.), AcroForm 89 fields (50ทวิ), html2canvas+jsPDF (ภพ.30/ภงด.)
- LINE notify: `lib/line.ts` server-side, silent fail เสมอ · **ค่าอยู่ใน `app_settings` ต่อ tenant ไม่ใช่ env** (0033/D51) — tenant มาจาก session เสมอ · **ห้ามใส่ fallback ไป env** (= ยิงเข้ากลุ่มลูกค้าคนอื่น)

## กติกาเหล็ก (business-critical)

1. **ห้ามแก้/“ปรับปรุง” สูตรคำนวณใด ๆ ที่มีผลต่อบัญชี/ภาษี/สรรพสามิต** โดยไม่เทียบ output กับระบบเดิม — ทุกสูตรมี unit test เทียบค่า (golden tests) ต้องผ่านก่อนถือว่างานจบ จุดที่ห้ามพลาดทั้งหมดอยู่ใน `docs/MIGRATION_PLAN.md` section 6 (P1-P12, A1-A16, S1-S8)
2. จุดที่อ่อนไหวที่สุด:
   - `lib/abv`: `ABV_CORR_TABLE` (ตาราง calal 41 แถวอุณหภูมิ × 101 ดีกรี) + `correctAbvTo20C` bilinear interpolation — **ห้ามพิมพ์ตารางใหม่ ห้าม reformat** นอกช่วงคืน `null` · golden test ~16k จุดต้องผ่าน 100%
   - **1 batch กลั่น = 1 แถว `log_distill`** (unique constraint) — ฟอร์ม ภส.๐๗-๐๒/๑(๑) หักส่าต่อแถว หลายแถว = หักซ้ำ = เลขยื่นราชการผิด
   - ภพ.30: VAT รวม = `round(Σยอด × 7/100 ×100)/100` จากยอดรวม (ไม่ sum vat รายแถว), filter เดือนด้วย `transaction_date`, แสดงวันที่ด้วย `tax_invoice_date`
   - Cash basis: ทุกรายงาน/ยอดเงิน ข้ามแถว `ap_ar_status is not null`
   - Stock: บวกเฉพาะ type `'รับ'` ที่เหลือลบหมด (รวม 'อื่นๆ' และ 'อื่น ๆ' สองแบบเว้นวรรค) — trigger บน `log_product`
   - สูตรถอด VAT ฝั่งขาย: `accPreVat = accNet / (1 + 0.07 − whtRate/100)` (ไม่มี WHT = `/1.07`)
3. **PDF ฟอร์มราชการห้ามเพี้ยนแม้ 1 mm** — copy พิกัด/ชื่อ field/ขนาดฟอนต์จากโค้ดเดิมเป๊ะ, ห้าม redesign, verify ด้วย pixel-diff กับ PDF จากระบบเดิม
4. ค่า enum ภาษาไทย ("รายรับ", "รับ", "จ่าย", "ปกติ", "ยกเลิก", สถานะออเดอร์ ฯลฯ) **คงภาษาไทยตามเดิม** — มี CHECK constraint ใน DB
5. เจอโค้ดเดิมขัดกับเอกสาร → **ยึดโค้ดเดิม** (`docs/legacy/`) แล้วบันทึกลง `docs/DECISIONS.md`
6. `SUPABASE_SERVICE_ROLE_KEY` ใช้ได้เฉพาะ migration script / legacy bridge — ห้ามหลุดเข้า client bundle

## กติกาการทำงาน

- งานถือว่าเสร็จเมื่อ: `npm run build` && `npm run lint` && `npm run test` ผ่านทั้งหมด แล้วให้ผู้ใช้เปิด `npm run dev` ตรวจใน browser
  · 🔴 **build/lint/test ผ่าน ≠ ผู้ใช้ใช้งานได้** (D68/D74/D77/D79/D80 พิสูจน์ซ้ำ ๆ) —
  เทสในเบราว์เซอร์จริงด้วยสกิล **`/browser-qa`** (`.claude/skills/browser-qa/`):
  สำรวจว่ามีอะไรควรเทส → เสนอแผน → ผู้ใช้ล็อกอินเอง → Claude ขับเทสจนจบลูป → รายงาน
- แก้ schema ผ่านไฟล์ migration ใน `supabase/migrations/` เท่านั้น (ห้ามแก้มือใน dashboard แล้วไม่จด)
  · มี **หลาย DB** แล้ว (ของเจ้าของ + ของลูกค้า) → ลง migration ด้วย `npm run db:push:all` (D57)
  ไม่ใช่ `supabase link` สลับทีละก้อน · ปริยายเป็น dry-run ต้อง `-- --apply` ถึงลงจริง
- **DB ทุกก้อนอยู่แผนฟรี = หลับเองถ้าไม่มีกิจกรรมใน 7 วัน** → ปิงวันละครั้งด้วย GitHub Actions +
  `npm run db:ping:all` (D60) · **รับ DB ก้อนใหม่ต้อง `npm run fleet:sync` แล้ว git push ด้วย**
  ไม่ใช่แค่เติม `targets.json` (ลืม = DB ลูกค้าที่จ่ายเงินแล้วหลับเงียบ ๆ) · 🚨 `supabase/fleet.json`
  อยู่ใน git ได้เพราะมีแค่ค่าสาธารณะ — **ห้ามเอา service role key / รหัส DB ลงไฟล์นั้นเด็ดขาด**
- Logic เงิน/ดีกรี/สต็อก อยู่ใน `lib/` หรือ Postgres function — ห้ามฝังใน component
- ไฟล์ยาวให้แตกตามโดเมนเหมือนโครงเดิม (`_js_*.html` เดิม → module แยก) — ผู้ใช้เคยเจ็บจากไฟล์ monolith มาแล้ว
- **Definition of Done ต่อ phase (ทำครบทุกข้อ):**
  1. `npm run build` && `npm run lint` && `npm run test` ผ่าน (สูตรเงิน/ภาษี/สรรพสามิตมี golden test เทียบระบบเดิม)
  2. อัปเดตตาราง progress ด้านล่าง + จด decision ใหม่ใน `docs/DECISIONS.md`
  3. **จดสิ่งที่ผู้ใช้ต้องทำตอน setup จริง** (ค่า config/ข้อมูลจริงที่ต้องกรอกเอง เช่น เลขภาษี/บัญชี/สิทธิ์) → เพิ่มหัวข้อของ phase นั้นใน `docs/GOLIVE_CHECKLIST.md`
  4. **ทำ/ต่อไกด์เทส** ใน `docs/TESTING.md` แบบ step-by-step (ทุกคำสั่งแยกบรรทัด + บอก "ควรเห็นอะไร") พร้อม seed + cleanup ใน `supabase/seed/` ที่ใช้ marker ลบทีเดียวได้ (entity ทดสอบ `EID99` · master id ขึ้นต้น `T-` · ชื่อ/หมายเหตุมีคำว่า "ทดสอบ") — ให้ผู้ใช้เทสเองได้โดยไม่ต้องคีย์ข้อมูลทีละอัน
- **ทุกจุดที่ผู้ใช้บันทึกข้อมูลได้ ต้องมีปุ่มแก้/ลบจากแอป** (role main) + ความสอดคล้องอัตโนมัติ (stock trigger ครอบ INSERT/UPDATE/DELETE) + audit `edit_log` — ดู FLOW_REDESIGN sec 10 (ผู้ใช้ย้ายมาจาก Sheets ที่แก้มือได้ทุกอย่าง — ห้ามทำให้ความสามารถนี้หาย)

## Sheet → Table mapping (สรุป — รายละเอียด+SQL เต็มใน MIGRATION_PLAN sec 2)

| ชีทเดิม | ตารางใหม่ | หมายเหตุ |
|---|---|---|
| Entities / Accounts / Users+inteam / Contacts / Settings | `entities` / `bank_accounts` / `profiles` / `contacts` / `app_settings` | core |
| Transactions (27 col) / Transaction_Items (11 col) | `transactions` / `transaction_items` | +`idempotency_key` unique |
| Tax_Summaries / pnd3-53 | `tax_summaries` / `wht_certificates` | tx_ids เป็น array · ~~Scan_Log~~ ตัดทิ้งแล้ว (D61) |
| API_Log (2 แอป) + acc_sync_queue | `integration_log` | คิว sync ถูกยุบ — insert ตรง |
| Master_Material / _Container / _Product | `materials` / `containers` / `products` | |
| Log_Material / Ferment / Distill / DistillRun / FermentMonitor / Dilute / Product | `log_material` / `log_ferment` / `log_distill` (batch UNIQUE) / `log_distill_run` / `log_ferment_monitor` / `log_dilute` / `log_product` | log_ferment คง matIds/matAmounts เป็น comma text |
| *(ไม่มีในระบบเดิม — เส้นทางสุราแช่ D78)* | `log_ferment_draw` (batch UNIQUE) | ริน+ปรุงน้ำสุราแช่ = 1 แถว/ครั้งที่หมัก |
| Stock_Product | `stock_product` | trigger + `recompute_stock_product()` + pg_cron weekly |
| btbtransaction (31 col) / btbsales / menu_b2b / curstock / stockmove | `sales_orders` / `sales_order_items` / `sale_menu` / `warehouse_stock` / `stock_moves` | |
| *(ไม่มีในระบบเดิม — มาจากแอปเงินเดือน GAS แยกโปรเจกต์)* | `employees` / `pay_inputs` / `pay_components` / `pay_rates` / `payroll_periods` / `payroll_items` | โมดูลที่ 4 (D66) · เกณฑ์อยู่ใน config ไม่ใช่โค้ด |
| custdata | ❌ ยุบเข้า `contacts` (เพิ่ม phone/email/credit_term/sale_name/is_export/roles) | ดู FLOW_REDESIGN sec 8 |

## Phases + สถานะ

| Phase | งาน | สถานะ |
|---|---|---|
| 1 | Scaffold + Supabase schema + RLS + Auth + login + Storage templates | ✅ เสร็จ + ผู้ใช้ setup แล้ว (login/RLS/templates ครบ) · +หน้าจัดการผู้ใช้ (username auth) |
| 2 | แอปผลิต (ทุกแท็บ + ABV golden test + ฟอร์ม ภส. 4 ตัว) | ✅ เสร็จ + ผู้ใช้เทสผ่าน (flow ผลิตครบ · PDF ภส. 4 ตัว อารบิก+เลขสรรพสามิต 17 หลัก · แท็บจัดการข้อมูล CRUD · กราฟติดตาม + หน้าประวัติเทียบหลาย batch) · ชุดเทส: `docs/TESTING.md` + `supabase/seed/` · ฟอนต์ = THSARABUN |
| 3 | แอปบัญชี (ทุกแท็บ + ภพ.30/ภงด./50ทวิ) | ✅ เสร็จ (รอผู้ใช้ push+test) · build/lint/test 102 ผ่าน · lib/accounting (calc/ledger/wht) golden A1-A11,A13 · RPC 0011 (save/installments/transfer/settle/void/issue-wht + T6 forward) · UI 8 แท็บ (entry/dashboard/บัญชี&เงินสด/AP-AR+ยอดค้างออเดอร์/ค้นบิล/แบ่งงวด/ประวัติราคา/เช็คราคา) · PDF: ภพ.30+ภงด. (HTML→print) · 50ทวิ (AcroForm 89 fields) ในแท็บเอกสารสรรพากร · ชุดเทส: seed_accounting.sql + docs/TESTING.md |
| 4 | แอปขาย (quotation/orders/warehouse + integrations) | ✅ เสร็จ (รอผู้ใช้ push+test) · build/lint/test 147 ผ่าน · lib/sales (calc/orders) golden S1-S8 (38 เทส) · RPC 0013 (quotation save/update · apply_order_action S2+RECEIVE_REVENUE idempotent · confirm_fulfillment S3+SELL_PRODUCT inline · manual_stock_move · cancel_order ย้อน side effect) · UI 4 แท็บ (สร้างใบเสนอราคา+ตะกร้า/จัดการออเดอร์ timeline+state machine 6 action/คลังจัดส่ง+สต็อกรวม/ประวัติเชื่อมระบบ) · พิมพ์: ใบเสนอราคา A4 + เอกสาร B2B (invoice/tax-invoice/receipt) client-side · docToPrint แก้ตาม D26 · config บัญชี+กิจการรับรายได้ (app_settings) · LINE `lib/line.ts` · ชุดเทส: seed_sales.sql + docs/TESTING.md |
| 5 | Migration scripts + reconcile | ✅ **รันจริงสำเร็จ** (2026-07-24) · reconcile 26/0 · build/lint/test 166 ผ่าน · `migration/` (lib clean/loader/client/transform + split/import/reconcile/export · npm `migrate:*`) · migration 0014-0017 · import จริง 468 tx/725 items/116 log/29 batch/contacts 35 · tz-safe date + พ.ศ.→ค.ศ. + remap ลูกค้า + contact ซ้ำ reassign + counters seed · +**type บันทึกภาษี** (D29) +**คู่ค้าหลายสาขา contact_id** (D30) +CLI บล็อก→apply ผ่าน dashboard (D31) · clean.test 15 เทส · D27-D31 · เหลือเทียบมือ: ยอดบัญชี+PDF ภพ.30/ภส. |
| 6 | UAT + shadow verification + cutover kit | ☐ |
| — | **ปรับหน้าตาเพื่อขายเป็นสินค้า — เฟส 1** (D43) | ✅ (2026-08-01) · build/lint/test **180** ผ่าน · migration **0022** (ต้อง `npm run db:push`) · design system "เหล็กกล้า" (`docs/DESIGN_SYSTEM.md`) · token สีทั้งแอป + โหมดสว่าง/มืด (cookie ไม่กะพริบ) · white-label 7 ชุดสี ตั้งจากแท็บตั้งค่า · ไอคอน SVG แทนอิโมจิ · ปุ่ม 7 สี→3 ระดับ · กราฟใช้ token · polish: component ตารางกลาง `.tbl` (33 ตาราง) · radius 3 ค่า · สีดิบเหลือ 0 · **เหลือ**: อัปโหลดโลโก้เข้า Storage (ยังไม่มีโลโก้) |
| — | **เอกสารการค้าอ่านข้อมูลผู้ขายจาก DB** (D44) | ✅ (2026-08-02) · build/lint/test **202** ผ่าน · migration **0023** (ต้อง `npm run db:push` ก่อนเปิดหน้าขาย) · เลิก hardcode ชื่อ/ที่อยู่/เลขภาษี/**เลขบัญชีธนาคาร** ใน `print.ts` → อ่านจาก `entities` (+`name_eng`/`phone`/`bank_line`) · `app_settings.sales_doc_entity` เลือกกิจการที่ออกเอกสาร (fallback `sales_revenue_entity` · หลายกิจการแล้วไม่ตั้ง = ไม่เดา เตือนแทน) · `lib/sales/company` + golden S9 22 เทส · UI การ์ด "ข้อมูลบนเอกสารการค้า" พร้อม**ตัวอย่างหัวกระดาษจริง** · ชุดเทส `docs/TESTING.md` ส่วนที่ 24 · **เหลือ**: โลโก้บนหัวเอกสาร (รอผู้ใช้มีโลโก้) |
| — | **ใบแจ้งหนี้ค่ามัดจำ** (D45) | ✅ (2026-08-11) · **ผู้ใช้เทสผ่านแล้ว** · build/lint/test **210** ผ่าน · migration **0024** (ต้อง `npm run db:push`) · action `ISSUE_INVOICE_DEPOSIT` + สถานะ `รอชำระมัดจำ` + เอกสาร `invoice-deposit` (หัว "ใบแจ้งหนี้" ธรรมดา + แถวยอดมัดจำ % + วันครบกำหนด) · **ไม่ลงบัญชีตอนออกบิล** (รับเงินจริงถึงออกใบกำกับ+ลงบัญชี ตามท่อเดิม) · คอลัมน์ `dep_*` แยกจาก `inv_no`/`doc_date1` · `fn_void_deposit_invoice` ย้อนกลับ `รอคอนเฟิร์ม` ได้ (role main) · golden S10 9 เทส · ชุดเทส `docs/TESTING.md` ส่วนที่ 25 |
| — | **ฐาน multi-tenant — ขายเป็นสินค้าได้จริง** (D46-D48) | ✅ (2026-08-11) · **merge เข้า main + push แล้ว (2026-08-12)** · migration **0025-0032** · test **252** + `npm run test:tenant` **79** (ยิง Supabase จริง) · `tenants` + `my_tenant()` + `tenant_id` 31 ตาราง (default+RLS → `.from()` 174 จุดไม่ต้องแก้) · `entity_id` 16 ตาราง + `my_default_entity()` · ผ่าตัด PK/unique **23 จุด** เป็น composite · RLS 56 policy · อุด definer 7 ตัว + service-role path (snapshot/restore เคยดูดข้ามลูกค้า) · หน้า login แสดงแบรนด์ตาม subdomain (co-brand) · แบรนด์แหล่งเดียวที่ `app_settings` (D47) · รหัสตั้งต้นสุ่มไม่ซ้ำ + บังคับเปลี่ยนครั้งแรก + **ชื่อผู้ใช้ห้ามซ้ำทั้งระบบ** (D48) · ชุดเทส `docs/TESTING.md` ส่วนที่ 26 · ตรวจโค้ดหลังผ่าตัด PK แล้ว **ไม่พบจุดพัง** (D49 · `tests/tenant/entity-scope.test.ts`) · **ผู้ใช้เทสในเบราว์เซอร์ผ่านครบแล้ว (2026-08-12)** · ✅ **ขั้น 6 ย้าย DB production เสร็จ (2026-08-12) — DB จริงอยู่ที่ 0032 แล้ว ข้อมูล 1,685 แถวตรงไฟล์สำรองเป๊ะ** (D50: migration ที่ backfill ต้องปิด user trigger) · ✅ **merge+push ขึ้น production แล้ว** · ❌ **MFA ตกไปแล้ว (D52)** |
| — | **ปิดรีวิว `docs/APP_REVIEW_2026-07.md` ครบทุกข้อ** (D36–D42) | ✅ D42 (2026-08-01) · build/lint/test **180** ผ่าน · migration **0021** (ต้อง `npm run db:push`) · multi-branch ครบ (รายรับขาย+50ทวิ เก็บ contact_id) · กระดาน batch + batch ร่วมข้ามแท็บ · แก้ inline log ผลิตครบ · checklist `report_runs` · `lib/shared/ui` + `billItems` (เลิกก๊อป 3 ชุด) · `lib/shared/paginate` + เทส · error.tsx ไทย · ฟอนต์ไทย self-host · PWA · ชุดเทส `docs/TESTING.md` ส่วนที่ 22 |

| — | **ขายเป็นสินค้า: LINE ต่อ tenant · โควตากิจการ · module flags** (D51-D53) | ✅ (2026-08-12) · test **249** + `npm run test:tenant` **79** · migration **0033** (LINE ต่อ tenant + ค่าลับใน app_settings อ่านได้เฉพาะ main) · **0034** (โควตา/โมดูล) · UI ซ่อนตัวเลือกกิจการเมื่อมีกิจการเดียว (★ ตัดสินจากจำนวน entity จริง **ห้ามผูกกับ max_entities**) · `workspacesFor(role, modules)` + `requireModule()` กัน URL ตรง · `npm run provision:tenant` / `provision:add-entity` (ระบบเปล่า ไม่มีข้อมูลตัวอย่าง) · `npm run backup:tables` · ❌ **MFA ตกไปแล้ว (D52)** · **เหลือ**: 4.3 VAT branching (ก้อนใหญ่สุด แตะชั้นสูตร) |

| — | **แอปจัดการหลังบ้าน (platform admin) เฟส 1** (D54) | ✅ (2026-08-13) · test **287** · migration **0035** (ต้อง `npm run db:push`) · route `/platform` (route group `app/(platform)`) — รับลูกค้าใหม่ · เปิด/ปิดโมดูล · ขยายโควตา · เพิ่มกิจการ · **รีเซ็ตรหัสลูกค้า** ได้จากหน้าจอ ไม่ต้องแตะ terminal/SQL · กัน 3 ชั้น: env `PLATFORM_ADMIN=1` (middleware → **404**) + ต้องล็อกอิน + uuid ต้องอยู่ใน `platform_admins` · 🚨 ตารางแพลตฟอร์ม **RLS deny-all + revoke grant** (ลืม = ข้อมูลเชิงพาณิชย์รั่วให้ลูกค้าทุกเจ้า) · ตรรกะแหล่งเดียว `lib/platform/provision.ts` (สคริปต์เดิมเรียกตัวนี้ · **ห้าม import server-only**) · รหัสชั่วคราวขึ้นจอครั้งเดียว ห้ามลง DB · `npm run platform:grant-admin` (bootstrap ครั้งเดียว) · ชุดเทส `docs/TESTING.md` ส่วนที่ 29 + `tests/tenant/platform-tables.test.ts` |

| — | **แอปจัดการหลังบ้าน เฟส 2 — ค่างวด + ระงับลูกค้า + เตือนในแอป** (D59) | ✅ (2026-08-17) · test **335** · migration **0037** (ต้อง `npm run db:push:all -- --apply` **ทุก DB** — ฝั่งลูกค้าอ่าน `tenants.billing_due_on` ด้วย) · `/platform/billing` เรียงครบกำหนดเร็วสุดก่อน + สรุปเลยกำหนด/ใกล้ครบ/รายได้ต่อเดือน · **ตัดรอบตามวันที่ลูกค้าเริ่ม sub** (anniversary) · 🪤 **วันตัดรอบคำนวณจากจุดยึด `started_on` + `periods_paid` เสมอ ห้ามบวกจาก `current_period_end`** (31 ม.ค. → 28 ก.พ. → ต้องกลับเป็น 31 มี.ค. ไม่งั้น drift ถาวรโดยไม่มีอะไรฟ้อง) · `status` ไม่มี `past_due` (คำนวณสด — ไม่มี cron มาพลิกค่า) · ระงับลูกค้าบังคับที่ `app/(app)/layout.tsx` **ไม่ใช่ RLS** (fail-open + ข้าม `is_platform`) → หน้า `/suspended` · 🚨 **ห้ามเปิด policy ให้ลูกค้าอ่าน `subscriptions`** (มีราคาต่อราย) — มิเรอร์เฉพาะวันครบกำหนดลง `tenants.billing_due_on` ด้วย trigger · เตือนในแอป: แถบ ≤3 วัน → ป๊อปอัพเมื่อเลยกำหนด · วันละครั้ง · เฉพาะ role `main` · ชุดเทส `docs/TESTING.md` ส่วนที่ 31 |

| — | **กัน DB แผนฟรีหลับ — ปิงทุกก้อนวันละครั้ง** (D60) | ✅ (2026-08-17) · test **360** · migration **0038** (ต้อง `npm run db:push:all -- --apply` ทุก DB ก่อนใช้) · `public.ping()` คืนแค่ `now()` เปิดให้ `anon` เรียก (🚨 ห้ามเติมความสามารถเข้าฟังก์ชันนี้) · `npm run db:ping:all` ปิงทุกก้อนในคำสั่งเดียว · ชั้นหลัก `.github/workflows/keep-db-awake.yml` วันละครั้ง 08:17 น. ไทย (workflow แดง = GitHub เมลเตือน — เหตุผลที่เลือกทางนี้แทน Vercel Cron ที่ Hobby เก็บ log แค่ 1 ชม.) · ชั้นสำรอง Task Scheduler `-- --notify` · 🪤 **สัปดาห์ละครั้งไม่พอ** เกณฑ์คือ "a few requests each day" ใน 7 วัน · 🪤 **pg_cron ยิงตัวเองไม่นับ** (ต้องเป็น user request จากข้างนอก) · รายชื่อ DB ที่ `supabase/fleet.json` (คอมมิต · สร้างด้วย `npm run fleet:sync` ห้ามแก้มือ) → `db:push:all` เตือนเองถ้ารับลูกค้าใหม่แล้วลืม sync · ชุดเทส `docs/TESTING.md` ส่วนที่ 32 |

| — | **4.3 VAT branching — กิจการไม่จด VAT** (D55) | ✅ (2026-08-14) · test **287** + `test:tenant` **102** · migration **0036** (ต้อง `npm run db:push`) · `isVat` ค่าปริยาย `true` ทุกฟังก์ชันสูตร → **golden S1-S10 เดิมผ่านโดยไม่แก้ไฟล์เทส** = หลักฐานว่าเส้นทางจด VAT ไม่ขยับ · WHT ยังคิดเสมอ (ไม่เกี่ยวกับการจด VAT) · 🚨 **บล็อกที่ DB ด้วย trigger** (ยิง API ตรงก็ไม่รอด) — ผู้ไม่จด VAT ออกใบกำกับภาษี = ผิด ม.86/13 · เอกสารเป็น "ใบเสร็จรับเงิน" · ซ่อนเฉพาะ ภพ.30 (**ภงด./50ทวิ ต้องคงไว้**) · ชุดเทส `docs/TESTING.md` ส่วนที่ 30 |

| — | **จัดบ้านหน้าตั้งค่า + ตัดสแกนใบเสร็จ + ยุบรายงานราชการ + ดร็อปดาวน์แถบบน** (D61-D65) | ✅ (2026-08-18) · test **377** · migration **0039** (ต้อง `npm run db:push:all -- --apply` ทุก DB — ลบตาราง `scan_log`) · **หน้าตั้งค่ากลาง `/settings` 5 แท็บ** (กิจการ/แบรนด์/แจ้งเตือน/ผู้ใช้/สำรองข้อมูล) — ย้ายแบรนด์+ข้อมูลเอกสาร+LINE ออกจากแท็บบัญชีที่ถูก `requireModule("accounting")` กั้น (ลูกค้าที่ซื้อแค่ผลิตเคยตั้งไม่ได้เลย) · 🪤 แยก `saveEntityInfoAction` / `saveDocEntityAction` — dropdown ตัวเดียวเคยทำ 2 หน้าที่ = แก้กิจการที่ 2 แล้วย้ายผู้ออกใบกำกับภาษีเงียบ ๆ · **เลขสรรพสามิตกรอกในแอปได้แล้ว** (`entities.excise_id` · ห้าม strip ขีดคั่น เตือนอย่างเดียวถ้าไม่ครบ 17 ตัว) · **ยุบ workspace รายงานราชการ** → แท็บ รายงานสรรพสามิต ในผลิต (🔴 pdf-lib ต้อง `await import()` ไม่งั้นทุกคนที่เปิดแอปผลิตโหลดตาม · ย้าย `getPdfAssetUrl` ไป `app/(app)/actions.ts` ก่อนลบโฟลเดอร์ ไม่งั้นบัญชี build พัง) · **ตัดสแกนใบเสร็จ AI ทิ้ง** (อ่านสลิปไทยไม่แม่นพอ · ลบ env `ANTHROPIC_API_KEY`/`SCAN_DAILY_LIMIT`) · **แท็บผูก URL `?tab=`** + ดร็อปดาวน์บนแถบเมนู · ทะเบียนแท็บกลาง `lib/shared/tabs.ts` + เทส 16 · ชุดเทส `docs/TESTING.md` ส่วนที่ 33 |

| — | **พ้นสภาพ/ปิดใช้งานแล้วต้องหลุดจากงวด + เอาคนออกจากงวดได้** (D76) | ✅ (2026-08-19) · test **566** · **ไม่มี migration** · 🚨 `employees.end_date` **เป็นช่องหลอก** — กรอกได้แต่ไม่มีโค้ดไหนอ่าน (ตระกูลเดียวกับ `expense_cat` ที่ลบใน D67) · `createPeriodAction` กรองด้วย `active` อย่างเดียว **และกรองแค่ตอนเติมคนใหม่** แถวที่เติมไปแล้วค้างตลอด · 🪤 **ใช้ธงอย่างเดียวไม่ได้ — คนลาออกกลางเดือนยังต้องได้เงินงวดนั้น** แต่ผู้ใช้ติ๊กออกไปแล้ว = จ่ายขาดคนโดยไม่มีอะไรฟ้อง → **วันที่ตัดสิน · ธงเป็นตัวสำรองตอนไม่ได้กรอกวัน** (`lib/payroll/employment.ts` + เทส 18) · แถวที่ค้างอยู่แล้ว **ไม่ลบให้เอง** แต่ติดป้าย ⚠ พร้อมเหตุผล + ปุ่ม **เอาออก** (งวดร่างเท่านั้น) · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.16 |
| — | **สุราแช่: เส้นทางผลิตที่ 2 + ฟอร์ม ภส.๐๗-๐๒/๑(๑) ฉบับสุราแช่** (D78) | ✅ (2026-08-24) · test **593** · migration **0045** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) + `npm run upload:templates` (ส่ง template ใบใหม่ขึ้น Storage) · 🎯 โรงเดียวขอผลิตได้ทั้งกลั่นและแช่ — สุราแช่ไม่มีการกลั่นและใช้**ฟอร์มบัญชีผลิตคนละใบ** · ⚠️ **หัวกระดาษพิมพ์เลขฟอร์มเดียวกันเป๊ะ** (ภส.๐๗-๐๒/๑(๑)) แต่คนละกระดาษ → `ExciseKind` + template key แยก · บนจอเรียก "บัญชีผลิตสุรากลั่น/สุราแช่" · ตารางใหม่ `log_ferment_draw` (ผู้ใช้เลือกไม่ reuse `log_distill` ทั้งที่ช่องตรงกันพอดี — ยอมโค้ด 2 ที่เพื่อแก้ดีเทลเฉพาะทางได้) · **ยกกติกาเหล็ก 1 batch = 1 แถว มาใช้** (หักน้ำหมักทั้งก้อนต่อแถว) · ธง กลั่น/แช่ = `products.liquor_type` ตัวเดิม เป็นดร็อปดาวน์ชุดปิด 🚨 **ห้ามใส่ CHECK constraint** (db:push:all ลงทุก DB = ล้มทั้ง fleet) 🚨 **ห้าม default เป็นกลั่น** เมื่อค่าว่าง (ออกฟอร์มผิดใบเงียบ ๆ) → เตือนบนจอแทน · ขั้นปรุงอยู่แถวเดียวกับการริน และ**ลงฟอร์มด้วยยอดหลังปรุง** (รวมที่ `drawnVol()` จุดเดียว — รอผู้ใช้ยืนยันกับสรรพสามิต) · ซ่อนแท็บของเส้นทางที่ไม่ได้ทำ ตัดสินจาก**สินค้าจริง** (🪤 เซ็ตว่าง = โชว์ครบ · ต้องกรองทั้งแถบแท็บและดร็อปดาวน์แถบเมนู) · 🔴 เจอเพิ่ม: `getPendingBatches`/`getBatchBoard` ตัดสิน "จบหรือยัง" จาก `log_distill` อย่างเดียว → batch สุราแช่ค้างตลอดกาล · 🪤 ช่องหมายเหตุมีข้อความผู้ใช้ปน = ยาวไม่จำกัด → ไหลออกนอกขอบกระดาษ ต้องมี `fit()` คุมความกว้าง · **ไม่แตะ `productionReport`** golden เดิมผ่านโดยไม่แก้ไฟล์เทส · ชุดเทส `docs/TESTING.md` ส่วนที่ 36 + `supabase/seed/seed_fermented.sql` |
| — | **ต้นทุนสุรา (บัญชี) → วัตถุดิบ (ผลิต) ไม่เคยทำงานเลยสักครั้ง** (D79) | ✅ (2026-08-24) · test **598** + `test:tenant` **134** · migration **0046** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🚨 `fn_save_transaction` เขียน `from jsonb_array_elements(p_items) it` โดยที่ `it` เป็นชื่อ**ตัวแปร plpgsql**ด้วย → `42702 ambiguous` และบรรทัดนั้นอยู่ **นอก** `begin/exception` → **บิลบัญชีไม่ถูกบันทึกด้วยซ้ำ** (ไม่ใช่แค่ forward พลาด) · บั๊กมาตั้งแต่ 0011 ก๊อปต่อใน 0017 = **ฟีเจอร์นี้ไม่เคยสำเร็จเลยตั้งแต่เปิดระบบ** (DB จริงไม่มีแถว `RECEIVE_MATERIAL` สักแถว) · 🪤 **ตรรกะที่อยู่ในฐานข้อมูล build/lint/test มองไม่เห็นเลย** — ชั้นเดียวที่จับได้คือ `npm run test:tenant` · 🔴 เจอเพิ่ม: (1) `fn_receive_material` ไม่ระบุ `entity_id` → ของไปลง**กิจการหลักเสมอ** แม้บิลลงกิจการที่ 2 (ตระกูล `apply_stock_delta` ที่ 0029 แก้ไปแล้ว — เอา entity จาก*เอกสาร* ไม่ใช่จาก*คนล็อกอิน*) 🪤 พารามิเตอร์เพิ่ม = ต้อง `drop function` ก่อน (D69) · (2) แถวที่กรอกแต่ราคาไม่กรอกชื่อ ล้ม forward ทั้งใบ · (3) **warning ขึ้นสีเขียว** เหมือนสำเร็จ → เพิ่มระดับ `warn` (เหลือง) ใน `Msg`/`useSaver` 🚨 **บันทึกได้บางส่วน ≠ สำเร็จ** · (4) `fn_mig_truncate` ตก `log_ferment_draw` (0045) + `snapshots` (0018) = **ลบ/รีเซ็ตลูกค้าล้ม** · `SNAPSHOT_ORDER`/`backup-tables` ก็ตก = **สำรอง/ย้อนข้อมูลแล้วของหายเงียบ ๆ** · กันซ้ำ: `lib/shared/tenantTables.ts` แหล่งเดียว + เทสไล่อ่านอีก 4 ที่ **รวมไฟล์ SQL** (พลาดมาแล้ว 3 รอบติด D67/D69/D78) · ⏳ **รอผู้ใช้ตัดสิน**: หมวดจุดชนวนยังฮาร์ดโค้ด `"ต้นทุนสุรา"` แต่ผังบัญชีจริงใช้ `"ค่าต้นทุนสินค้า"` · ชุดเทส `docs/TESTING.md` ส่วนที่ 37 |
| — | **บั๊กจากการใช้แอปจริงแบบลูกค้าใหม่ — ชุด A+B+C ครบ 7 ข้อ** (D80) | ✅ (2026-08-25) · test **620** + `test:tenant` **139** · migration **0047-0048** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 เจอจากการไล่ใช้แอปตั้งแต่ตั้งค่าจนจบทุกลูปในเบราว์เซอร์ — **build/lint/test เดิมผ่านหมด 100% ทุกข้อ** · 🚨 **A1 ชื่อบนเอกสารยื่นเป็นชื่อเก่า**: ภงด.1 พิมพ์ชื่อที่แช่ไว้ใน `payroll_items.emp_name` คู่กับ**เลขบัตรจากทะเบียนปัจจุบัน** = ชื่อกับเลขประจำตัวเป็นคนละคนบนแบบที่ยื่นสรรพากร · สลับข้างจากกติกา D75 (D69 เขียนก่อน D75 กติกาใหม่กวาดไม่ถึง) กระจาย 5 จุด → `empDisplayName()` ตัวเดียว (ทะเบียน > snapshot > ว่าง) · 🪤 **ยกเว้น 50ทวิ ที่ออกแล้ว** คงชื่อ ณ วันออก (ใบอยู่ในมือพนักงานจริง) · 🚨 **เทสเดิมล็อกพฤติกรรมผิดไว้** (เคส "ชื่อล่าสุดชนะ") ต้องแก้เทสด้วย · 🚨 **A2 ข้อมูลหลักไม่มี audit + `edit_log` ไม่มีที่ให้ดู**: ไม่มี trigger บน `products`/`entities`/`materials`/`containers`/`contacts`/`bank_accounts`/คอนฟิกเงินเดือน · และ `edit_log` **ไม่มีหน้าจอเลยแม้แต่หน้าเดียว** ทั้งที่หน้าแก้บิลเขียนบอกผู้ใช้ว่า "บันทึกใน edit_log" = บอกว่าเก็บไว้แต่ไม่บอกว่าดูที่ไหน → หน้าใหม่ **ตั้งค่า → ประวัติการแก้ไข** (ดูอย่างเดียว + คัดลอกค่าเก่า · 🚨 ไม่มีปุ่มเขียนทับ DB · 🚨 ห้ามเทดัมพ์ JSON ดิบ → `changedFields()` โชว์เฉพาะช่องที่ต่างพร้อมชื่อไทย) · 🪤 **`trg_audit` ต้องเอา tenant จากแถวเอง** ไม่งั้นผูก trigger กับ `entities` แล้ว `provision:tenant` (service role) ล้ม = **รับลูกค้าใหม่ไม่ได้เลย** (ตระกูล D50/0029) · 🚨 **ห้าม audit `app_settings`** (มีโทเคน LINE = ค่าลับรั่วลง edit_log) · `AUDITED_TABLES` + เทสไล่อ่าน `create trigger audit_…` จากไฟล์ SQL มาเทียบ · 🚨 **B1 ลูกค้าใหม่ปิดการขายใบแรกไม่ได้**: `sales_revenue_entity`/`sales_revenue_account` **ไม่มีที่ไหนเขียนเลยสักจุดในทั้งระบบ** ตั้งได้ทางเดียวคือยิง SQL และ error ยังชี้ให้ไปเปิดไฟล์เอกสาร → การ์ดที่ 3 ในหน้า ตั้งค่า → กิจการ (บัญชีเป็นดร็อปดาวน์จาก `bank_accounts` · 🪤 แยก action จาก `saveDocEntityAction` ตามกับดัก D63) · 🚨 **B2 หมวดจุดชนวนรับวัตถุดิบตั้งเองได้** (migration **0048**): เดิมฮาร์ดโค้ด `ต้นทุนสุรา` แต่ผังบัญชีจริงใช้ `ค่าต้นทุนสินค้า` → kind ใหม่ `material_forward_cat` หลายหมวดได้ · **ค่าปริยายเปลี่ยนเป็น `วัตถุดิบผลิตสุรา`** · ไม่มีแถว = ใช้ค่าปริยายในโค้ด (ไม่ต้อง seed) · 🪤 **`app_settings.kind` เป็น CHECK whitelist** เพิ่มในโค้ดอย่างเดียวไม่พอ ต้องยกรายชื่อเดิมมาครบใน migration · 🚨 **บั๊กที่เกิดระหว่างแก้แล้วจับได้ตอนเทส**: โชว์ค่าปริยายเป็น chip = ผู้ใช้นึกว่าบันทึกแล้ว พอเพิ่มตัวที่ 2 ค่าปริยายหลุดเงียบ ๆ (ตระกูล D74) → แยก `forwardCatsSet` (ที่ตั้งเอง) จาก `forwardCats` (ที่มีผล) + ข้อความเหลืองบอกสถานะ 🪤 **ค่าปริยายที่คำนวณในโค้ด ห้ามแสดงปนกับค่าที่บันทึกไว้จริงในช่องที่แก้ได้** · 🚨 **C1 ขนาดขวดกรอกเป็น มล. ได้เงียบ ๆ** → ภส.๐๗-๐๒/๑(๒) คิดลิตร = ขวด × ขนาดขวด กรอก 330 แทน 0.33 = ปริมาณบนเอกสารราชการผิด **1,000 เท่า** → `bottleSizeWarn()` เตือนเมื่อ > 5 ล. (โชว์บนแถวที่ยังไม่กดแก้ด้วย ไม่งั้นของเดิมที่ผิดไม่มีวันถูกเห็น) + แถบเตือนรวมในแท็บรายงานสรรพสามิต · 🚨 **เตือนอย่างเดียว ไม่แปลงหน่วยให้** (ผู้ใช้ตัดสิน — ถังใหญ่ 20 ล. จะโดนหารด้วย) · 🚨 **C2 แท็บกลั่นเลือก batch สุราแช่ได้** และปิด batch ได้จริง = ยอดโผล่ในฟอร์มบัญชีผลิต**สุรากลั่น**ผิดใบ (ฝั่งรินน้ำสุราแช่กรองถูกตั้งแต่ D78 — สองข้างไม่สมมาตร) → ติดธง `fermented` ใน `getPendingBatches` แล้ว**กรองที่ DistillTab** 🪤 **ห้ามกรองใน `getPendingBatches` ตรง ๆ** เพราะใช้ร่วมกับแท็บติดตามหมัก (สุราแช่ก็ต้องวัด pH/Brix) 🪤 `liquor_type` ว่าง = ไม่ซ่อน (การซ่อนก็คือการเดา) · 🚨 **C3 คอลัมน์ Yield ขึ้น "—" ตลอดกาล**: คอลัมน์ `log_distill_run.ferm_charge` มี · `startDistillRunAction` รับพารามิเตอร์แล้ว · สูตร+golden test มี — **แต่หน้าจอไม่เคยส่งค่าเลยสักครั้ง** (ตระกูล D74/D77) → เพิ่มช่อง "น้ำหมักที่เข้าหม้อ" คู่ปุ่มเริ่มหม้อ (จังหวะที่แถว "เริ่มกลั่น" ถูกสร้างจริง) · ชุดเทส `docs/TESTING.md` ส่วนที่ 38 |
| — | **เตือนกำหนดยื่นงบเดือนสรรพสามิตเข้า LINE** (D92) | ✅ (2026-09-01) · test **914** · **ไม่มี migration** · **ไม่ต้องแก้ `vercel.json`** (route เดิม · cron เดิม · `CRON_SECRET` ตัวเดิม) · 🎯 ต่อจาก D91 ทันที — ปุ่มปิดเดือนช่วยได้เฉพาะตอนเปิดแอป ไม่ได้เข้าเลยทั้งเดือนก็เลยกำหนดยื่นแล้วค่อยรู้ (เหตุผลเดียวกับ D88) · ★ **เป็นการเพิ่มผู้เข้าร่วมรายที่สอง ไม่ใช่สร้างระบบใหม่** — cron + `sendLineToTenant()` + กันซ้ำด้วย `integration_log` มีครบแล้ว · `integration_log.action` เป็น text อิสระ (ไม่มี CHECK) และ unique index เป็น `(tenant_id, action, idempotency_key)` แยกต่อลูกค้าอยู่แล้วตั้งแต่ 0027 จึงไม่ต้องมี migration · 🚨 **ภพ.30 ครบกำหนดยื่นกระดาษวันที่ 15 เหมือนงบเดือนสรรพสามิตเป๊ะ → วันเตือนชนกันทุกเดือนตลอดไป** → ผู้ใช้เลือก **แยก 2 ข้อความ** เพราะเป็น**คนละกรมและคนละสิ่งที่ต้องไปกด** (สรรพากร→สร้างแบบ · สรรพสามิต→ปิดเดือน) รวมกันแล้วท้ายข้อความจะมีคำสั่ง 2 อัน · และไม่ต้องแตะโค้ด D88 ที่เทสผ่านแล้ว · **เตือนเฉพาะกิจการที่กรอก `entities.excise_id`** — schema `0001` เขียนกำกับไว้เองว่า *"เฉพาะโรงสุรา"* → เป็นธง "ต้องยื่นงบเดือน" ที่ตรงที่สุดและผู้ใช้คุมเองได้ · หลักเดียวกับ ภพ.30 ที่เตือนทุกเดือนที่จด VAT แม้ไม่มียอด · 🔴 **ผลข้างเคียงที่ต้องมีทางออก**: ไม่กรอกเลข = ไม่ได้รับการเตือนเลย ซึ่งเป็น**ความเงียบที่ผู้ใช้ไม่มีทางรู้เอง** (เจอจริงตอนเทส: ลูกค้า 2 รายที่ซื้อโมดูลผลิตยังไม่ได้กรอก) → การ์ดปิดเดือนขึ้นบรรทัดเหลืองบอกตรง ๆ พร้อมบอกว่าไปกรอกที่ไหน 🚨 **ทุกครั้งที่ระบบไม่ทำอะไรให้ ต้องบอกว่าทำไม** · 🚨 **จุดเสี่ยงที่สุด: `continue` ของงานเก่ากลืนงานใหม่** — บล็อกเตือนภาษีของ D88 มี `continue` **4 จุด** (ไม่มีกิจการ · อ่าน wht ไม่ได้ · ไม่มีอะไรต้องเตือน · ส่งไปแล้ว) เอางานใหม่ไปต่อท้ายในลูปเดียวกัน = **ถูกข้ามเงียบ ๆ และ TypeScript มองไม่เห็นเลย** → แยกเป็น `taxPart()` / `excisePart()` เรียกตามธงโมดูลแยกกัน (`accounting` / `production`) · ลูกค้าที่ซื้อแค่ผลิตเดิม**ไม่ได้อะไรเลย**เพราะโค้ดเก่าตัดทิ้งตั้งแต่บรรทัดแรก · 🪤 **เทสข้อ "ส่งก่อนจด" เขียนพลาดรอบแรกแล้วผ่านด้วยเหตุผลผิด** — ค้นชื่อ constant ทั้งไฟล์แล้วเจอ**บรรทัด import** ก่อน ทำให้ลำดับถูกโดยบังเอิญ → แก้เป็นตัดเฉพาะตัว `excisePart` มาตรวจ · **เทสที่ผ่านโดยไม่ได้ตรวจอะไร อันตรายกว่าไม่มีเทส เพราะสร้างความมั่นใจปลอม** · กติกาที่ยกจาก D88 ทั้งดุ้น: **ไม่บอกยอดเงิน** · **ส่งก่อนแล้วค่อยจด** (จดก่อนแล้วส่งพลาด = เตือนหายตลอดกาล) · **ไม่เลื่อนวันหยุดราชการ** · **อ่าน `error` ทุกจุด** (อ่านสถานะปิดเดือนไม่ได้แล้วเดาว่า "ยังไม่ปิด" = สแปมเตือนทั้งที่ปิดแล้ว) · `tenantId` มาจากตาราง `tenants` เท่านั้น · ที่**ต่างจาก D88 โดยตั้งใจ**: **บอกวันเดียว ไม่มีวันยื่นออนไลน์** (ไม่รู้ **และไม่เดา**) · จำนวนวันในหัวข้อความคิดจาก `leadDays` ไม่ฮาร์ดโค้ด · ย้าย `nextMonth`/`prevMonth`/`thaiDay` ไป `lib/shared/period.ts` (🚨 ห้ามให้ `lib/production` import `lib/accounting`) แล้ว `taxPay.ts` re-export ต่อ → ไฟล์อื่นไม่ต้องแก้ · 🚩 **golden A17 (35) + A18 (17) ผ่านโดยไม่แก้ไฟล์เทส** · ✅ **พิสูจน์ด้วย dry-run ของ cron จริงครบทุกทิศ**: วันที่ 12 → เตือน · เดือนที่ปิดแล้ว → เงียบ **แต่ฝั่งภาษียังยิงครบ** (พิสูจน์ว่า 2 งานอิสระกันจริง) · วันอื่น → ไม่มีอะไร · ข้ามปี ธ.ค.→ม.ค. ถูก · **ถอนปิดเดือนแล้ววันเดิมที่เคยเงียบกลับมาเตือน** · จดว่าส่งแล้วยิงซ้ำ → ไม่ส่งซ้ำ · ชุดเทส `docs/TESTING.md` ส่วนที่ 49 |
| — | **ปิดเดือนสรรพสามิต — แยก "เช็กลิสต์" ออกจาก "ตัวล็อก"** (D91) | ✅ (2026-09-01) · test **882** · migration **0058** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 **แก้ข้อผิดพลาดเชิงออกแบบของ D90 ที่เพิ่งจบไปไม่กี่ชั่วโมง** — ผู้ใช้ทักว่ากติกาใช้กับงานจริงไม่ได้: *"บัญชีที่ไม่ใช่งบเดือนมีการกดออกรายงานอยู่ตลอด เพราะหากสรรพสามิตเข้ามาตรวจ โรงงานต้องมีเอกสารรายวันให้ดู"* · 🚨 **D90 ถามผิดตาราง**: `report_runs` ถูกสร้างมาเป็น**เช็กลิสต์** (คอมเมนต์ใน 0005 และ `ExciseTab.tsx:13` เขียนไว้เองว่า *"เดือนนี้สร้างครบยัง"* · append-only · index `created_at desc`) แต่ D90 เอาไปตอบคำถาม *"ยื่นไปแล้วหรือยัง"* → บัญชีประจำวันถูกพิมพ์แทบทุกวัน = **ยกเลิกบิลแล้วแก้ฟอร์มไม่ได้ตลอดกาล** ทั้งที่งบเดือน (๐๗-๐๔) ซึ่งเป็นใบที่*ยื่น*จริงยังไม่ได้ส่ง · 🚩 **บทเรียน: ตารางเดียวทำสองหน้าที่** (ตระกูล D84/D63) — การกด **"พิมพ์"** กับการ **"ยื่น"** เป็นคนละเหตุการณ์ 🪤 **ตารางที่มีข้อมูลอยู่แล้วพอดี ไม่ได้แปลว่ามันตอบคำถามที่เรากำลังถาม** (`report_runs` มี entity·month·ชนิดฟอร์ม ครบพอดี แต่ความหมายของแถวคือ *กดปุ่มแล้ว* ไม่ใช่ *ยื่นแล้ว*) · ★ ผู้ใช้เสนอ 3 ทางมาเอง แล้วเลือก **ทางที่ 3 (พรีวิว→คอนเฟิร์มปิดเดือน) เป็นแกน กลืนทางที่ 1 (ปุ่มคืนค่า) เป็นการถอนปิด** — ทางที่ 1 เดี่ยว ๆ ไม่พอเพราะ 🪤 **ปุ่มที่ต้องกดเป็นกิจวัตรคือปุ่มที่วันหนึ่งจะลืมกด แล้วตัวเลขผิดเงียบ ๆ** · ทางที่ 2 เดี่ยว ๆ ก็ยังถือว่า "พิมพ์ = ยื่น" → กดดูงบเดือนกลางเดือนเพื่อเช็กตัวเลข = ล็อกทันที · **`report_runs` กลับไปเป็นเช็กลิสต์อย่างเดียว ไม่ถูกแตะแม้บรรทัดเดียว** (D88 ยังใช้เป็นเงื่อนไข *ต้องสร้างแบบก่อนถึงจ่ายภาษีได้*) · ตารางใหม่ `excise_month_close` **1 แถวต่อรอบการปิด** ถอนปิด = เติม `reopened_at` **ไม่ลบแถว** (ตอบได้ว่าใครปิด ใครถอน เมื่อไร) + partial unique index `where reopened_at is null` (แพตเทิร์น `tax_payments` D88) · **ไม่มี policy เขียน** เขียนผ่าน RPC definer เท่านั้น (บทเรียน D85/0052 `for all` ครอบ SELECT) · 🚨 **RLS เป็น `prod.read` ล้วน ไม่ใช่ `prod or acct`** — ★ ต่างจาก `report_runs` ที่ 0055 ต้องเปิดให้บัญชี **เพราะตารางนั้นเก็บ 2 โดเมนปนกัน** ตารางนี้โดเมนเดียว (เขียนเหตุผลกำกับกันคนก๊อป 0055 มาโดยไม่ดูบริบท) · 🚨 **คำถามอยู่ที่เดียว** `fn_excise_months_open()` เรียกทั้งจาก `fn_cancel_order` และ `fn_excise_recompute_hidden` (D79/D85) · 🚨 **คู่ จ่าย/รับ ข้ามเดือนได้** → ต้องเปิดครบ**ทุกเดือนที่แถวแตะ** · ซ่อนข้างเดียว = ยอดคงเหลือบนฟอร์มเพี้ยน **แย่กว่าไม่ซ่อนเลย** · ★ ไล่เดือนจาก**แถวจริง** ไม่ใช่ `current_date` แบบ D90 (ตรงตอนยกเลิก แต่ผิดตอนคำนวณใหม่ทีหลัง) · 🚨 **สองกฎกลับด้านกันและถูกทั้งคู่**: **ถอนปิด → คำนวณใหม่ทันที** (ปุ่มคืนค่าที่ผู้ใช้ขอ ไม่ใช่ให้ไปกดอีกปุ่ม) แต่ **ปิดเดือน → ห้ามคำนวณใหม่เด็ดขาด** (คู่ที่ซ่อนไว้ตอนเดือนเปิดต้องซ่อนต่อ ไม่งั้นแถวโผล่กลับมาบนฟอร์มที่กำลังจะยื่นพอดี) · `totals` เก็บ **ผลรวมข้อมูลขาเข้าฟอร์ม ไม่ใช่สำเนาแบบที่ยื่น** (เรนเดอร์ครบทุกใบ = เรียกหลายสิบครั้ง · ผลรวมขาเข้าจับได้ทุกความเปลี่ยนแปลงด้วย query เดียว) · `driftSummary()` คืนว่างเสมอเมื่อยังไม่เคยปิด (🪤 D88 ข้อ 4: *"ต่างจากยอดที่ยื่นไว้"* เคยโผล่ทั้งที่ยังไม่เคยยื่น) · 🚨 **ไม่ backfill อะไรทั้งสิ้น** (ไม่มีบันทึกว่าเดือนไหนยื่นจริง) → ปุ่ม **คำนวณใหม่ตามจริง** ให้กดรายเดือนโดยเห็นจำนวนคู่ที่กระทบก่อน · **ปิดเดือนไม่บล็อกการแก้ข้อมูล** เตือนอย่างเดียว (ผู้ใช้ย้ายมาจาก Sheets — CLAUDE.md) · 🔴 **เจอ cap ที่หายไปตั้งแต่ D85**: `CAPS` มี `acct/sales/pay × read/write/config` ครบ แต่ผลิตมีแค่ read/write → เติม `prod.config` · ★ **`has_cap()` ฝั่ง SQL ไม่ต้องแก้เลย** (`main → true` ทุก cap · บทบาทอื่นไม่มีชื่อนี้ในลิสต์) → `rolesSql.test.ts` ผ่านโดยไม่แก้ไฟล์เทส · เหตุผลที่ใช้ `*.config`: ยึดตาม D85 ที่วางไว้ว่า **ยกเลิกออเดอร์ = `sales.config`** (ระดับหัวหน้า) · การ์ดปิดเดือนอยู่**นอก** `ReportChecklist` เพราะบัญชีใช้คอมโพเนนต์ตัวเดียวกัน · ป้าย/ข้อความทุกตัวตัดสินใน `lib/production/monthClose.ts` ที่มีเทสคุม **ห้ามตัดสินในคอมโพเนนต์** (D84/D88) · เทสที่ล็อกกติกา D90 ด้วย `report_runs` **ถูกแก้ให้ล็อกกติกาใหม่** (ล็อก*ที่มาของคำตอบ*ที่เปลี่ยนไปจริง) + ข้อใหม่ที่จับได้ว่า `fn_cancel_order` ต้องไม่ query `report_runs` · **ปิดเดือนต้องไม่เรียก recompute** · ไม่มี backfill ระดับ migration · ตารางใหม่อยู่ก่อน `entities` ใน `fn_mig_truncate` · `tests/tenant/excise-month-close.test.ts` 🔴 ชั้นเดียวที่เห็น plpgsql (D79) รวมข้อที่เป็นต้นเรื่องตรง ๆ *กดสร้างรายงานแล้วยกเลิก → ต้องยังซ่อนได้* · migration ยก `fn_cancel_order` (0057) + `fn_mig_truncate` (0054) มาด้วยสคริปต์ + `diff` เทียบ (กัน D79) 🪤 เจอระหว่างทาง: `\_` ใน template literal ของ JS **ถูกกลืน backslash ทิ้ง** → สคริปต์หาบล็อกเดิมไม่เจอแล้ว throw ให้เห็นทันที (ตระกูล `$` ใน heredoc ของ D85 · เหตุผลที่ยกด้วยสคริปต์แทนพิมพ์มือ) · ชุดเทส `docs/TESTING.md` ส่วนที่ 48 · ✅ **เทสในเบราว์เซอร์ครบทุกข้อแล้ว — เจอบั๊ก 1 ตัวที่ build/lint/test เดิมผ่านหมด (migration 0059)**: เปิดเดือนที่มีคู่ซึ่ง**ถูกซ่อนอยู่** แต่เดือนที่ขายปิดไปแล้ว → หน้าจอขึ้นว่า *"ที่ยังแสดงบนฟอร์ม … กดเพื่อเอาออก"* ซึ่ง**กลับด้านกับความจริงพอดี** (คู่นั้นถูกซ่อนอยู่ และการกดจะทำให้**กลับมาแสดง** ซึ่งถูกต้องเพราะฟอร์มที่ยื่นไปมีแถวนั้นอยู่) · 🚨 **ตรรกะทำถูกทุกประการ ที่ผิดคือประโยคที่ผู้ใช้อ่านแล้วกดปุ่ม** · สาเหตุ: dry-run คืนแค่ `changed` = ตัวเลข**ไร้ทิศทาง** แล้วหน้าจอเดาเองว่าทิศคือ "ซ่อนเพิ่ม" เสมอ ทั้งที่ recompute ตั้งใจให้เป็นสองทางมาตั้งแต่ 0058 🪤 **ตัวเลขที่ไม่มีทิศทาง ห้ามเอาไปแต่งประโยคที่มีทิศทาง** (ตระกูล D81 แต่กลับด้าน — โค้ดถูก คำอธิบายผิด ซึ่งอันตรายพอกันเพราะผู้ใช้ทำตามประโยค) · เทสเดิมไม่จับเพราะตรวจแค่ว่าประโยคมีตัวเลขถูก **ไม่ได้ตรวจว่าทิศที่ประโยคบอกตรงกับทิศที่ปุ่มจะทำ** → `to_hide`/`to_show` แยกตั้งแต่ชั้น SQL + เทสอ่านซอร์สหน้าจอว่าต้องไม่เหลือ `pendingHide` ให้เดาเอง · ★ **พิสูจน์ 2 ทิศทางบนข้อมูลจริง**: กดพิมพ์ 10 ครั้งแล้วยกเลิก → ยังซ่อนได้ (ข้อที่เป็นต้นเรื่อง) · ปิดเดือนแล้วยกเลิก → ไม่ซ่อน + แถบเหลืองบอกว่าใครต้องกดอะไร · คู่ข้ามเดือนไม่ถูกซ่อนครึ่งเดียวทั้งสองทิศ · สุทธิ `log_product` เท่ากันเสมอ · `stock_product` ตรงทุกสินค้า · test **890** + `test:tenant` **207** |
| — | **ยกเลิกออเดอร์แล้วไม่โผล่บนฟอร์มสรรพสามิต** (D90) | ✅ (2026-09-01) · test **854** + `test:tenant` **191** · migration **0057** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 **ผู้ใช้เอา PDF ภส. ไปตรวจด้วยตาแล้วทักว่าตัวเลขไม่ตรง** — ออเดอร์ที่ยกเลิกโชว์เป็น *จ่าย 4 แล้วรับคืน 4* ครบคู่ ทั้งที่สุทธิไม่มีอะไรเกิดขึ้น · 🚩 **ผมตอบไปว่านั่นถูกแล้ว (บันทึกตามจริง) — ผู้ใช้ไม่เห็นด้วย และผู้ใช้ถูก**: "บันทึกตามจริงทุกแถว" เป็นกติกาของ*ฐานข้อมูล* แต่ **ฟอร์มที่ยื่นราชการคือเอกสารที่มีคนต้องอ่านแล้วกระทบยอด** แถวที่หักล้างกันเองในวันเดียวกันคือเสียงรบกวน · 🚨 **กติกาที่ผู้ใช้แก้ให้: ตัดสินจาก "ออกรายงานไปหรือยัง" ไม่ใช่ "เดือนเดียวกันไหม"** (ผมเสนอกฎตามปฏิทิน ผู้ใช้แก้เพราะ *"รายงานสรรพสามิตส่งได้ภายในวันที่ 15 ของเดือนถัดไป"* → ยกเลิก 3 ต.ค. ของบิล ก.ย. ยังทันถ้ายังไม่ยื่น) แหล่งความจริงคือ `report_runs` ที่มีอยู่แล้วจาก D42 · 🚨 **ตัดสิน ณ ตอนกดยกเลิกแล้วแช่ผลไว้ในคอลัมน์ ห้ามตัดสินตอนเปิดดูฟอร์ม** — คิดสดตอน render แล้วพอเดือนนั้นถูกออกรายงาน*ทีหลัง* แถวจะโผล่กลับมาเอง = ฟอร์มเปลี่ยนย้อนหลังอีกแบบ ซึ่งคือสิ่งที่ตั้งใจกันตั้งแต่แรก (ตระกูล D75/D69 *ตัวเลขที่ยื่นแล้วต้องเป็นค่าที่แช่ไว้*) · เช็ค **ทั้งเดือนที่ขายและเดือนที่ยกเลิก** (ขาย ก.ย. ยื่นแล้ว · คืน ต.ค. → ต้องโชว์ทั้งคู่) · 🚨 **ซ่อนเฉพาะ "ฟอร์ม" ห้ามลามไปสต็อก** — `excise_hidden` ถูกอ่านที่ `excise-data.ts` ที่เดียว · **trigger สต็อกไม่ถูกแตะแม้บรรทัดเดียว** (เผลอกรองที่ trigger = ของหายจากสต็อกจริงเงียบ ๆ — บทเรียน D82 เป๊ะ) · หน้าสต็อก/ประวัติในแอปยังเห็นครบตามจริง · ★ **ซ่อนเป็นคู่ที่หักล้างกันพอดีเสมอ** (`ref_no` เดียวกัน) → ยอดคงเหลือบนฟอร์มยังตรงกับสต็อกจริงทุกกรณี · 🪤 **เลิกให้รายงานแกะเลขออเดอร์จากข้อความหมายเหตุ** (มีข้อความที่ผู้ใช้แก้ได้ปนอยู่) → คอลัมน์ `log_product.ref_no` เขียนโดย RPC ทั้งสองฝั่ง · backfill จากหมายเหตุครั้งเดียวตอนติดตั้ง · 🚨 **ไม่ backfill `excise_hidden` ย้อนหลัง** — ฟอร์มเดือนเก่าอาจยื่นไปแล้ว (เทสนับว่าต้องมี `update … excise_hidden` **จุดเดียว** คือในตัว RPC) · `exciseHidden.test.ts` **อ่านซอร์ส+SQL จริงมาตรวจ** (ชั้นเดียวกับ `tenantTables.test.ts` D79 / `rolesSql.test.ts` D85) เพราะกติกาอยู่ 2 ฝั่งที่ TS มองไม่ทะลุ — SQL แช่ค่า · TS เชื่อค่า **หลุดข้างใดข้างหนึ่งไม่มี error ทั้งคู่** · migration ยก `fn_confirm_fulfillment`/`fn_cancel_order` จาก 0051 มาด้วยสคริปต์ ไม่พิมพ์มือ (กัน D79) · ✅ **เทสในเบราว์เซอร์ 2 ทิศทาง เดือนเดียวกัน สินค้าเดียวกัน ต่างกันแค่เงื่อนไขเดียว**: `ORD260901-003` ยกเลิกตอนออกรายงานแล้ว → `false` ทั้งคู่ (ฟอร์มที่ยื่นไม่ขยับ) · `ORD260901-004` ยกเลิกก่อนออกรายงาน → `true` ทั้งคู่ · **ปิดลูปถึงตัวเลขที่ออกจากโค้ดจริง**: payload ที่ป้อนเข้า pdf-lib ของ ภส.๐๗-๐๒/๑(๒) คืน `รับ 5 · จ่าย 5 · คงเหลือ 88` อ้างอิง `-001, -002, -003` **ไม่มี -004 ปรากฏที่ใดเลย** · `stock_product` เทียบผลรวม `log_product` ทุกแถวรวมที่ซ่อน ยังตรงทุกสินค้า · ชุดเทส `docs/TESTING.md` ส่วนที่ 47 · ⚠️ **จุดชนวนถูกแทนที่แล้วใน D91** — เลิกดู `report_runs` (เช็กลิสต์) เปลี่ยนเป็นดู `excise_month_close` (ปิดเดือน) · กติกาอื่นของแถวนี้ยังใช้อยู่ทั้งหมด |
| — | **อ่านข้อมูลไม่สำเร็จต้องฟ้อง + เลขใบเสร็จผู้ไม่จด VAT** (D89) | ✅ (2026-09-01) · test **845** · migration **0056** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 **ข้อ 1**: ต้นเรื่องคือ `searchBills` ทิ้ง `error` แต่ไล่จริงพบเป็นแพตเทิร์นทั่วชั้นอ่าน **82 จุด ใน 6 ไฟล์** · 🚨 **จุดหนักสุดไม่ได้อยู่ในบัญชี/ขาย**: ฟอร์ม ภส. ที่ยื่นสรรพสามิตขาดแถวเงียบ ๆ · เลข 50ทวิ ออกซ้ำ · ภงด.1/สปส. ยื่นขาดคน · ภาษีซื้อยกมาหายทั้งก้อน · **จ่ายภาษีซ้ำ** (D88 เพิ่งเพิ่มจุดนี้มา) · **เลข batch วนกลับไป 1 → ชน batch เดิม** (กติกาเหล็ก) → เรียงแก้ตามความอันตราย ข้ามโดเมน · ★ **บทเรียนนี้จดไว้แล้ว 2 รอบ (D85, D88)** — 🪤 **จดไว้ไม่ได้แปลว่าแก้แล้ว ต้องมีกลไกที่ทำผิดซ้ำไม่ได้** → `mustRead()` (ยึดแพตเทิร์น `fetchAllRows` ที่ throw อยู่แล้ว) + `mapDbError` เพิ่ม SQLSTATE ฝั่งอ่าน 5 ตัว (รวม `22008` = เคสจริงของ D88) + `readGuard.test.ts` **อ่านซอร์สจริงมาตรวจ** (พิสูจน์แล้วว่าจับได้) · 🚩 **กฎเหล็กของงานนี้: แก้ data กับ UI ต้องมาคู่กัน** — ทั้งแอปมี `.catch()` อยู่จุดเดียว ทำให้ชั้นอ่าน throw โดยไม่แก้ UI = **ค้างที่ "กำลังโหลด…" ตลอดกาล ซึ่งแย่กว่าลิสต์ว่างเดิม** → `useRead()` + `<LoadError>` (เก็บข้อมูลเดิมไว้ ไม่ล้างทิ้งตอน error · 🪤 Next.js ปิดบังข้อความ error ของ server action ใน production จึงต้องใช้ข้อความไทยของเราเอง) · 🚨 **จุดที่ตั้งใจให้ทนพังห้ามแตะ**: `if (!so.error)` ของหน้าลูกหนี้-เจ้าหนี้ (ลูกค้าที่ซื้อแค่บัญชี) · fail-closed ของ `profiles.role` · fail-open ของธงโมดูล — **มีเทสล็อกไว้** · ⚠️ `loadTaxAccounts` มี fallback ที่**ดูปลอดภัยแต่ไม่ใช่** (โรงที่ตั้งชื่อบัญชีอื่นจะถูกกรองออกจน ภพ.30 = 0) 🪤 **fallback ที่ดูสมเหตุสมผลคือที่ซ่อนตัวได้ดีที่สุด** · 🎯 **ข้อ 2**: D86 แก้เลขใบเสร็จผู้ไม่จด VAT ไปครึ่งเดียว เหลือ **ใบเสร็จมัดจำ = เลขว่าง** และ **ใบเสร็จยอดค้าง = ซ้ำเลขใบแจ้งหนี้** → คอลัมน์ `rcpt_no1`/`rcpt_no2` เป็น**คู่ขนาน**ของ `tax_no1`/`tax_no2` (ช่องรับเงิน 2 ช่องเท่ากันทั้งสองโลก ต่างแค่ใช้เลขชุด `INV`) · 🚨 **ไม่แตะ trigger `block_tax_invoice_non_vat`** — ด่านกฎหมาย ม.86/13 ต้องแข็งเหมือนเดิม (มีเทสคู่ positive/negative ใน `test:tenant`) · 🪤 สาขาไม่จด VAT ต้องคิดจาก `rcptNo*` **ไม่ใช่ `taxNo*`** ที่ว่างเสมอ ไม่งั้นขอเลขใหม่ทุกครั้งที่กด · ★ ถอด "การยืมช่อง inv" ของ D86 ออก (เทสที่ล็อกวิธีแก้ชั่วคราวถูกแก้ด้วย) · ★ แทนที่จะอ้างว่า "เทสเดิมไม่ถูกแก้" ใช้ **ตารางล็อกครบ 7 action** พิสูจน์ว่าเส้นทางจด VAT ไม่ขยับ · migration ยก `fn_apply_order_action` มาด้วยสคริปต์ + `diff` เทียบ (กัน D79) · ชุดเทส `docs/TESTING.md` ส่วนที่ 46 |
| — | **ขายหน้าร้าน (POS) — กดครั้งเดียวจบ** (D86) | ✅ (2026-08-31) · test **766** · migration **0053** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 ขายหน้าโรงงาน 1-2 ลัง ไม่ต้องเสนอราคา/วางบิล/รอคลังแพ็ก — เดิมต้องเดิน 3 หน้าจอ ≈ 12 คลิกต่อบิล 300 บาท · ★ **โฟลว์มีครบอยู่แล้ว แค่กระจาย 3 หน้าจอ** — `FULL_PAYMENT_AND_SEND` ตั้ง `docToPrint = tax-invoice-receipt-do` (*ใบกำกับภาษี/ใบเสร็จรับเงิน/ใบส่งสินค้า*) อยู่แล้ว · `fn_confirm_fulfillment` ตัดทั้ง `warehouse_stock` และ `log_product` (เข้าฟอร์ม ภส.) → **งานนี้คือการรวบโฟลว์ ไม่ใช่การเขียนสูตรใหม่** (`lib/sales/calc.ts` ไม่ถูกแก้แม้บรรทัดเดียว · golden S1-S10 ผ่านโดยไม่แก้ไฟล์เทส) · 🚨 **จงใจไม่ทำ RPC ก้อนเดียว** — อะตอมมิกกว่าก็จริง แต่ต้องยกตรรกะเลขเอกสาร+ถอด VAT ไปเขียนใน SQL อีกชุด = **สูตรเงินมี 2 ที่** (กลไกเดียวกับ D79) → เรียก RPC เดิม 3 จังหวะต่อกัน · 🪤 **ล้มกลางทาง = สำเร็จบางส่วน ห้ามขึ้นสีเขียว** → ระดับ `warn` พร้อมบอกเลขออเดอร์และแท็บที่ต้องไปกดต่อ (ทุกสถานะกลางเป็นสถานะที่ถูกของโฟลว์เดิมและมีหน้าจอรองรับอยู่แล้ว) · 🔴 **เจอบั๊กเก่าตั้งแต่ D55**: กิจการไม่จด VAT ออกใบเสร็จแล้วได้เอกสาร **ไม่มีเลขที่** (`neededSerials` ตัด tax ทิ้งถูกแล้ว แต่ action นั้นมี `inv: false` อยู่แล้ว) → `tax_invoice_no` เป็น `"-"` · แก้ขอบเขตแคบให้ใช้เลขชุด `INV` · เส้นทางจด VAT ไม่ขยับ (`taxNo1 || invNo`) · 🪤 `app_settings.kind` เป็น CHECK whitelist ที่เขียนทับทั้งก้อน + `app_setting_cap()` ค่าปริยายเป็น `admin` (ไม่ประกาศ = หัวหน้าฝ่ายขายตั้งลูกค้าทั่วไปไม่ได้) · 🪤 **เมนูที่ไม่ผูก `product_id` ถูกข้ามเงียบ ๆ ตอนตัดสต็อก** → POS ซ่อนเมนูพวกนั้น **แต่ต้องบอกว่าซ่อนกี่ตัวและทำไม** (D83) · 🪤 `stockQty = null` = **ไม่เตือน** (ไม่รู้ ≠ ไม่มี) · 🚨 **ขายย้อนวันไม่ได้** (`log_product.doc_date = current_date` → วันบนฟอร์ม ภส. ไม่ตาม) · 🔴 **เจอบั๊กเก่าอีกตัว**: ฟอร์มเพิ่มลูกค้าบังคับเลขภาษี 13 หลักทุกกรณี → **ลูกค้าส่งออกสร้างจากหน้าจอไม่ได้เลย** (ผู้ซื้อต่างชาติไม่มีเลขภาษีไทย · seed ถึงต้องสร้างด้วย SQL — ตระกูล D74) → เช็คบ็อกซ์ **ไม่มีเลขประจำตัวผู้เสียภาษี** · ไม่ติ๊ก = บังคับเหมือนเดิม (คุณภาพข้อมูล B2B ไม่ตก) · 🪤 `isExport` **ผ่อนกฎอย่างเดียว ไม่ล้างช่อง** (นิติบุคคลไทยที่ซื้อไปส่งออกมีเลขจริง) ส่วน `noTaxId` ซ่อนช่อง+บันทึกค่าว่าง **รวมถึงสาขา** (ปล่อยเป็น `สำนักงานใหญ่` = ใบกำกับขึ้น `- (สำนักงานใหญ่)` ขัดกันเอง) · 🐛 เลือก “สาขา” แล้วไม่กรอกเลข ของเดิมตรวจ**ค่าหลัง `padStart`** → `""` กลายเป็น `00000` ผ่านเงียบ ๆ แล้วพิมพ์ “(สาขาที่ 00000)” ลงใบกำกับ (00000 = รหัสสำนักงานใหญ่) · กติกาย้ายไป `lib/sales/customer.ts` golden **S13** 17 เทส · ★ **ชื่อ “ลูกค้าทั่วไป” ไม่ได้ฮาร์ดโค้ด** — `pos_walkin_contact` เก็บ `contact_id` ลูกค้าแต่ละเจ้าตั้งชื่อเองได้ · 🔴 **เจอ 2 บั๊กทันทีที่ผู้ใช้ลองใช้จริง (build/lint/test เดิมผ่านหมด)**: (1) ปุ่ม *ตั้งเป็นลูกค้าทั่วไป* ถูก `&&` ซ่อนจนกว่าจะเลือกลูกค้า = **หายไปพอดีตอนที่ผู้ใช้กำลังหามันอยู่** (ตระกูล D83 แต่กลับด้าน — ซ่อนปุ่มแย่กว่าปุ่มเทา) → render เสมอแล้ว disable + `<MissingHint>` · แยก `setupMode` ออกจาก *เปลี่ยนลูกค้าเฉพาะบิลนี้* (การ์ดเดียวทำ 2 หน้าที่ = กับดัก D63) · เพิ่มคู่ค้าใหม่ตอนตั้งค่าครั้งแรก = ตั้งให้เลย (2) *ขายแล้วบิลไม่เข้าบัญชี* — **จริง ๆ เข้าแล้ว** แต่ `searchBills` เรียงด้วย `transaction_date` อย่างเดียวไม่มีตัวตัดสินรอง → บิลวันเดียวกันออกมาไม่กำหนดลำดับ บิลขายไปแทรกแถวที่ 9 กลางบิลเงินเดือน 8 ใบ แล้วผู้ใช้สรุปว่าข้อมูลหาย → เพิ่ม `.order(created_at desc)` 🪤 **ลำดับที่ไม่แน่นอนทำให้ผู้ใช้เชื่อว่าข้อมูลหาย แล้วกดขายซ้ำจนได้บิลซ้ำจริง** · ชุดเทส `docs/TESTING.md` ส่วนที่ 44 + `supabase/seed/seed_pos.sql` |
| — | **ชำระภาษี ภพ.30/ภงด.3/ภงด.53 + เตือนกำหนดยื่นเข้า LINE** (D88) | ✅ (2026-08-31) · test **817** · migration **0054** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 ยอดที่ต้องจ่ายคำนวณได้อยู่แล้ว แต่ **ไม่มีที่ไหนบันทึกว่าจ่ายไปแล้ว** → เงินออกจริงแต่ยอดเงินสดในแอปไม่ขยับ · กันจ่ายซ้ำไม่ได้ · ★ **ขอบเขตมีแค่ 3 แบบ** — 🚨 `ภงด.1`/`สปส.` มีปุ่มจ่ายอยู่แล้วที่โมดูลเงินเดือน (`pay_post_legs` D67) **เติมที่หน้าบัญชีอีก = ลงรายจ่ายซ้ำ ไม่มีอะไรใน DB ฟ้อง** (เทสล็อกรายชื่อไว้) · ภงด.3 กับ ภงด.53 **ยื่นแยก จ่ายแยก** 2 ใบ แต่เตือนรวมบรรทัดเดียว · 🚨 **แช่ยอดในแถวของตัวเอง ห้ามผูก FK ไป `tax_summaries`** — `recordTaxSummaryAction` **ลบแถวเดิมทิ้งแล้ว insert ใหม่ทุกครั้งที่กดสร้าง ภพ.30** และมีปุ่มลบให้ผู้ใช้กดเองอีก (ตระกูล D75) · 🚨 **ไม่ลอก idempotency ของเงินเดือนมา เพราะมันมีบั๊ก** — `fn_unpost_payroll` ทิ้งแถว `integration_log` ไว้ → **ถอนแล้วลงใหม่ = ไม่เกิดอะไรเลยแต่ตอบ ok** · ที่นี่กันซ้ำด้วย **partial unique index ของตารางเอง** (`where status=ปกติ`) · ★ **ไม่ทำหน้าตั้งค่าเพิ่ม** — ป๊อปอัพจ่ายคือที่ตั้งค่า แล้วจำค่าครั้งก่อนของแบบนั้นมาเติมให้ · 🚨 **ต้องกดสร้างแบบก่อนถึงจ่ายได้** บล็อกที่ DB ด้วย (ยิง API ตรงก็ไม่รอด) · 🚨 **เบี้ยปรับ/เงินเพิ่มแยกบิลคนละหมวดเสมอ** (รายจ่ายต้องห้าม ต้องบวกกลับสิ้นปี) · จ่าย = `acct.write` · **ถอน = `acct.config`** (soft-void ไม่ลบ) · 🪤 **คนยกเลิกบิลจากหน้าค้นบิลได้** → อ่าน `transactions.status` มาด้วย ขึ้นป้ายแดง *บิลถูกยกเลิก* แล้วเปิดให้จ่ายใหม่ (แสดงความจริง ไม่ซ่อนและไม่แก้ให้เอง) · กิจการไม่จด VAT: แถว ภพ.30 **หายไปเลย** ไม่ใช่โชว์แล้วเทา · **ภงด.3/53 ยังต้องมีครบ** · 🪤 บิลจ่ายภาษี `vat_amount=wht_amount=0` → ไม่วนกลับเข้ารายงานเดือนถัดไป (ล็อกใน RPC + ไกด์เทสให้ไปดูด้วยตา) · **เตือน LINE**: cron `/api/cron/tax-reminder` (Vercel cron + `CRON_SECRET`) ล่วงหน้า 3 วัน · 🚨 **ไม่บอกยอดเงิน** บอกแค่ต้องยื่นอะไรภายในวันไหน (+ กำหนด e-Filing เสมอ เพราะไม่รู้ว่ายื่นแบบไหน) · ภพ.30 เตือน**ทุกเดือนที่จด VAT แม้ไม่มียอด** · ภงด. เตือนเฉพาะเดือนที่หักจริง · กันซ้ำด้วย `integration_log` = **ครั้งเดียวต่อแบบต่องวด** · 🪤 **ส่งก่อนแล้วค่อยจด** (จดก่อนแล้วส่งพลาด = เตือนหายตลอดกาล) → `sendLineToTenant()` คืน boolean · 🚨 เป็น**ข้อยกเว้นเดียว**ของกติกา "tenant มาจาก session เสมอ" — tenantId ต้องไล่จากตาราง `tenants` เอง ห้ามรับจากคำขอ · 🪤 **middleware เด้ง request ไร้ session ไป /login** → ไม่ยกเว้น `/api/cron/*` = cron ได้ 200 พร้อม HTML หน้า login **งานเงียบหายโดยไม่มี error** · ไม่ตั้ง `CRON_SECRET` = **503 ปิดตาย ไม่ใช่เปิดฟรี** · 🚨 **ไม่เลื่อนวันหยุดราชการให้** (ไม่มีปฏิทิน · เตือนเร็วไปดีกว่าเดาแล้วช้า) · golden **A17** 31 เทส + **A18** 17 เทส · ชุดเทส `docs/TESTING.md` ส่วนที่ 45 + `supabase/seed/seed_taxpay.sql` · ✅ **เทสในเบราว์เซอร์จริงครบทุกลูปแล้ว** — build/lint/test เดิมผ่านหมดแต่ยังเจอบั๊ก **4 ตัว** (test **821**): (1) 🔴 **บิลถูกยกเลิกจากหน้าค้นบิล = ทางตัน** หน้าจอบอก *กดบันทึกจ่ายใหม่ได้* แต่ DB กันอยู่ (แถว `tax_payments` ยังเป็น `ปกติ`) แถมปุ่มถอนก็หายไปด้วย → **"จ่ายแล้ว" มี 2 นิยามที่ไม่ตรงกัน** (ตระกูล D81) → `canUnpay()` แยกจาก `canPay()` และรวมกรณีบิลถูกยกเลิก 🪤 **ทุกครั้งที่ปิดปุ่ม ต้องตอบให้ได้ว่าผู้ใช้ต้องกดอะไรแทน** (2) 🔴 **cron เตือน ภงด. ไม่เคยทำงานใน 5 จาก 12 เดือน** — `${month}-31` ได้ `2026-11-31` ที่ไม่มีอยู่จริง → error 22008 → `data` null → เซตว่าง **เงียบสนิทเพราะไม่ได้อ่าน `error`** 🪤 ปลายช่วงต้องเป็นวันที่ 1 ของเดือนถัดไป + `lt` · 🚨 **อ่านไม่ได้ ≠ ไม่มีข้อมูล** (3) 🟡 **ยอดยกไปโชว์ค่าสด** (629.02) แทนค่าที่แช่ไว้ (658.46) ทั้งที่ค่าที่แช่ไว้คือตัวที่เดือนถัดไปอ่านไปใช้จริง — ไม่ตรงกับตารางในหน้าเดียวกัน (ตระกูล D75) (4) 🟡 **ข้อความ "ต่างจากยอดที่ยื่นไว้" โผล่ทั้งที่ยังไม่เคยยื่น** + ป้าย *ไม่มียอด* ทั้งที่มียอดรออยู่ → `drifted` เฉพาะเมื่อ `filed` + ป้าย `badge` ย้ายไปตัดสินใน lib ที่มีเทสคุม (บทเรียน D84) · ★ **พิสูจน์ 2 ทิศทาง**: ยอดบัญชีลดลง 391.54 พอดี · ภพ.30 เดือนถัดไปภาษีซื้อ **0.00** (บิลจ่ายภาษีไม่วนกลับ) · ยกไป 0.00 → เดือนถัดไปยกมา 0.00 · เปิด 2 แท็บกดจ่ายซ้ำได้บิลใบเดียว · เบี้ยปรับแยก 2 บิลคนละหมวด · **ถอนแล้วจ่ายใหม่ได้บิลใหม่จริง** · (5) 🔴 **บั๊กเก่าจาก D85 ที่งานนี้ไปเปิดโปง (migration 0055)**: `report_runs` ถูกจัดไว้ในลูป *ตารางฝั่งผลิต* ของ 0051 → policy เป็น `prod.read/write` เท่านั้น แต่ตารางนี้เก็บ **2 โดเมนปนกัน** (ฟอร์ม ภส. + ภพ.30/ภงด.) → **ฝ่ายบัญชีอ่านไม่ได้ เขียนไม่ได้ เงียบสนิท** เช็กลิสต์ขึ้น *ยังไม่ได้สร้าง* ตลอดกาล และ D88 ทำให้**ปุ่มบันทึกจ่ายล็อกถาวรทุกบทบาทฝ่ายบัญชี** 🪤 `rolesSql.test.ts` จับไม่ได้ (ตรวจแค่ ROLE_CAPS ↔ has_cap **ไม่ได้ตรวจว่าตารางไหนควรเปิดให้ cap ไหน**) · 🪤 บั๊กซ้อน: `markReportRunAction` คืน error อยู่แล้วแต่ `TaxDocsTab` ไม่เคยอ่าน + ฝั่ง ภงด. ติ๊กเช็กลิสต์เองใน client → **หน้าจอโกหกทับบั๊กอีกชั้น** → อ่านผลจริง + ขึ้น warn เหลือง + เลิกติ๊กเอง |
| — | **สิทธิ์ 9 บทบาท — capability แทนรายชื่อ role** (D85) | ✅ (2026-08-27) · test **713** + `test:tenant` **184** · migration **0051** (ลง DB ครบทั้ง 2 ก้อนแล้ว) · 🎯 ของเดิม 4 บทบาท **กัน "แก้" แน่นแต่แทบไม่กัน "ดู"** — `policy select` ส่วนใหญ่เขียน `using (auth.uid() is not null)` = ใครล็อกอินก็อ่านหมด ทั้งบิลบัญชี · ราคาทุน · สูตรการผลิต · ราคาขายลูกค้าทุกราย (กันจริงมีแค่เงินเดือนกับ `edit_log`) · **9 บทบาท** = โดเมน × ระดับ (main · viewer · sales_manager/sales · finance_manager · accounting_manager/accounting · payroll_manager/payroll) · `sale`+`warehouse` ยุบเป็น `sales` (🚨 คนคลังเดิม**ได้สิทธิ์เพิ่ม**) · 🚨 **`my_role()='main'` เคยถูกก๊อป 150 จุดใน 15 ไฟล์ migration** = เพิ่มบทบาทใหม่ต้องไล่แก้ทุกจุดและลืมแล้วไม่มีอะไรฟ้อง (กลไกเดียวกับ D84) → เปลี่ยนเป็นถาม **cap** 12 ตัว (`prod/acct/sales/pay` × `read/write/config` + `admin`) · `Record<Role,…>` ทั้ง `ROLE_CAPS`/`ROLE_LABEL`/`ROLE_HINT` → เพิ่มบทบาทแล้วลืมกำหนดสิทธิ์ = **build ไม่ผ่าน** · 🚨 **ตารางสิทธิ์มี 2 ฝั่ง** (`ROLE_CAPS` ฝั่ง TS คุมหน้าจอ · `has_cap()` ใน SQL คือตัวจริง) หลุดจากกันแล้ว**ไม่มี error ทั้งคู่** → `rolesSql.test.ts` **อ่าน SQL เป็นข้อความมาเทียบ** (ชั้นเดียวกับ `tenantTables.test.ts` ของ D79) · 🪤 **`prod.read` เกือบทำ 2 หน้าที่ปนกัน** (เข้าหน้าผลิต vs อ่านแคตตาล็อก) → ฝ่ายขายไม่มี `prod.read` แต่ `products`/`stock_product` เปิดให้ `prod.read` **หรือ** `sales.read` · สูตร/ค่าดีกรียังปิดสนิท · 🚨 **RPC 8 ตัวเป็น definer = bypass RLS** ต้องเช็คเอง (บทเรียน 0028→0029) รวม ~473 บรรทัด → **ยกมาด้วยสคริปต์ `gen-0051.mjs`** ไม่พิมพ์มือ (กัน D79 ซ้ำ) · ★ `fn_save_transaction`/`fn_issue_wht` **ไม่ต้องแตะ** เพราะเป็น invoker · 🪤 อ่านข้ามโดเมน 3 จุดแก้ที่โค้ด ไม่ใช่เปิด policy กว้าง (เลข 50ทวิ พนักงานใช้ชุดเดียวกับคู่ค้า → RPC `fn_wht_doc_nos` คืนเฉพาะเลขที่) · 🚨 **ยกเลิกออเดอร์ = `sales.config`** (หัวหน้าเท่านั้น — void ใบกำกับภาษีที่ออกไปแล้ว) · 🚨 `toRole()` fallback เป็น **viewer ห้ามเป็น main** (ขอบเขตความปลอดภัย fail-closed ต่างจากธงโมดูลที่ fail-open) · 🪤 **"อ่านไม่ได้" ในโลก RLS = คืนลิสต์ว่าง ไม่ใช่ error** — เทสต้อง assert จำนวนแถว · จับได้ว่าเทสตัวเองผ่านเพราะ**ไม่มีข้อมูล** ไม่ใช่เพราะสิทธิ์ถูก · 🪤 `$$` ใน heredoc โดนกลืนเป็น `$` (migration ล้ม · CLI rollback ให้ ไม่เสียหาย) แล้ว `String.replace()` ก็แปลง `$$`→`$` ซ้อนอีกชั้น · ชุดเทส `docs/TESTING.md` ส่วนที่ 43 · ✅ **เทสในเบราว์เซอร์ครบ 9/9 บทบาทแล้ว** — เจอบั๊กที่ build/lint/test เดิมผ่านหมด **2 ตัว**: (1) 🔴 **ทะเบียนแท็บกรองสิทธิ์ถูก แต่ 3 ใน 4 หน้าจอไม่เรียกตัวกรอง** (`AccountingApp`/`PayrollApp` map จาก `*_TABS` ตรง ๆ · `ProductionApp` ฮาร์ดโค้ด `"main"`) = พนักงานบัญชีเห็นแท็บตั้งค่า (ตระกูล D68/D74 — ของมีครบทุกชั้น ขาดชั้นที่เอามาใช้จริง) → เทสอ่านซอร์ส 4 App มาตรวจ · (2) 🚨 **`for all` ครอบ SELECT ด้วย** และ policy permissive ถูก **OR กัน** → `wht_w` (pay.write) เปิดอ่านทุกแถวทับ `wht_sel` ที่แคบกว่า = **ใบ 50ทวิ ของคู่ค้ารั่วให้ฝ่ายเงินเดือน** (migration **0052** ยกเงื่อนไข `emp_id` ไปฝั่งเขียนด้วย) 🪤 ตารางอื่นรอด**โดยบังเอิญ** เพราะบทบาทที่มี `.write` มี `.read` ของโดเมนเดียวกันเสมอ · (3) 🟡 ข้อความบนจอเขียนชื่อบทบาทตายตัว ("ต้องเป็น main") = คำโกหกที่รอวันเกิด → ตัดสินจาก cap + แสดง `ROLE_LABEL` · ★ **พิสูจน์ 2 ทิศทางทุกข้อ** (ไม่ใช่ซ่อนหมดแล้วผ่าน): manager เห็นแท็บตั้งค่า**และเพิ่ม/ลบค่าได้จริง** · `sales_manager` **ยกเลิกออเดอร์สำเร็จ** · ฝ่ายเงินเดือนมองไม่เห็นใบคู่ค้าแต่**เลขยังเดินต่อถูก** (6905→6906) · test **731** + `test:tenant` **188** |
| — | **หน้าแอดมินโชว์ชื่อโมดูลผิด — ซื้อ "เงินเดือน" ขึ้นเป็น "ขาย"** (D84) | ✅ (2026-08-26) · test **665** · **ไม่มี migration** · 🎯 เจอตอนเปิด `/platform` ครั้งแรกด้วยตา (build/lint/test เดิมผ่านหมด) — ตารางลูกค้าโชว์ `demoprov` เป็น *ผลิต · บัญชี · ขาย · **ขาย*** ทั้งที่ DB เก็บ `[production, accounting, sales, payroll]` ถูกต้อง · 🚨 สาเหตุ: `{m === "production" ? "ผลิต" : m === "accounting" ? "บัญชี" : "ขาย"}` เขียนตอน D54 ที่มี 3 โมดูล พอ **D66 เพิ่มโมดูลที่ 4** มันตกเข้า else กลายเป็น "ขาย" เงียบ ๆ · 🪤 **ternary ไล่เช็ค key = else ที่กลืนของใหม่ทุกอัน** ต่างจาก `Record<Key,string>` ที่ TS บังคับให้ครบ · 🪤 **ในไฟล์เดียวกันมี `MODULE_LABEL` ที่ถูกครบ 4 ตัวอยู่แล้ว** แต่ใช้แค่ในช่องติ๊ก = ความจริง 2 แหล่งในไฟล์เดียว อัปเดตไปแหล่งเดียว · 🚨 **หน้านี้คือหน้าที่ใช้ตอบว่าลูกค้าซื้ออะไรก่อนเก็บเงิน** — อ่านผิด = เก็บเงินผิดแพ็กเกจ / บอกลูกค้าว่าไม่มีของที่จ่ายไปแล้ว · ★ **สิทธิ์จริงไม่เคยผิด** (`workspacesFor`/`requireModule` ใช้ key ไม่ใช่ป้าย) เสียแค่ฝั่งที่มนุษย์อ่าน · แก้: ย้ายชื่อไป `MODULE_LABEL: Record<ModuleKey, string>` ใน `lib/shared/workspaces.ts` บ้านเดียวกับ `MODULES` → **เพิ่มโมดูลแล้วลืมเติมชื่อ = build ไม่ผ่าน** · หน้าแอดมินเหลือ `moduleLabel()` ตัวเดียว · คำต่อท้าย "(+ ฟอร์ม ภส.)" แยกเป็น `MODULE_NOTE` · เทส +4 (รวมข้อ **ชื่อห้ามซ้ำกัน** = อาการของบั๊กนี้เป๊ะ) · ✅ **เทสในเบราว์เซอร์จริงครบ** รวมโควตากิจการเต็ม/ลด/ขยาย (คืนค่าแล้ว ยืนยันที่ DB) · ชุดเทส `docs/TESTING.md` ส่วนที่ 42 |
| — | **ปุ่มที่ถูก disable ต้องบอกว่า "ยังขาดอะไร"** (D83) | ✅ (2026-08-26) · test **661** · **ไม่มี migration** · 🎯 เจอจาก `/browser-qa` — ปุ่ม *ออกใบเสนอราคา (A4)* เทาเฉย ๆ เพราะขาด **ผู้เสนอราคา** ที่อยู่ล่างสุดของการ์ด ไม่มีอะไรบอก → ผู้ใช้ใหม่นึกว่าปุ่มเสีย (ตระกูล D74/D77/D80 · build/lint/test ผ่านหมดเพราะโค้ดทำถูกตามที่เขียน) · `missingText()` ใน `lib/shared/forms.ts` + `<MissingHint>` ใน `lib/shared/ui.tsx` (re-export ผ่าน barrel 3 โดเมน) ขึ้นบรรทัดเหลืองใต้ปุ่ม *"ยังกรอกไม่ครบ: ลูกค้า · ผู้เสนอราคา"* · ครบแล้วไม่ render อะไรเลย · 🚨 **เป็นแค่คำอธิบาย ไม่ใช่ตัวตัดสิน** — `disabled=` ของปุ่มยังเป็นตัวจริง จงใจไม่ผูกเป็นตัวเดียวกัน (รวมแล้วแก้ข้างเดียว = ปุ่มกดไม่ได้แต่บอกว่าครบ) · ไล่ `disabled=` ครบทั้ง 32 จุด → เติม hint 12 component · เติม `title=` ที่ปิดเพราะ**กฎ** (PK แก้ไม่ได้ · ลบตัวเองไม่ได้ · ปุ่มเติมทุกแถวว่าง = ล้างค่าเดิม) · **ไม่แตะ**หน้าที่มีคำอธิบายอยู่แล้ว (PeriodTab · EntryTab · platform quota · เมนูสินค้าที่ยังไม่เลือกลูกค้า) · 🪤 การ์ดหน้าตั้งค่าวางปุ่มใน `div.flex` → hint ต้องอยู่**นอก** `</div>` ไม่งั้นไปโผล่ข้างปุ่ม · ชุดเทส `docs/TESTING.md` ส่วนที่ 41 · ✅ **เทสในเบราว์เซอร์จริงแล้ว ไม่เจอบั๊ก** (41.7 · รวมกับดักแท็บ mount ค้าง: DOM มี hint 3 ตัว มองเห็นจริงตัวเดียวถูกแท็บ) |
| — | **เลิก snapshot ในแอป → ดาวน์โหลดข้อมูลเก็บเอง** (D82) | ✅ (2026-08-25) · test **656** + `test:tenant` **138** · migration **0049-0050** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · 🎯 มาจากคำถามผู้ใช้ว่า snapshot กระทบลูกค้าเจ้าอื่นไหม → **ฝั่งข้อมูลแยกครบทุกบรรทัด** แต่ `restoreSnapshot()` เรียก `fn_mig_set_triggers(false)` ซึ่ง**ไม่มีพารามิเตอร์ tenant** (Postgres ปิด trigger ได้ระดับตารางเท่านั้น) = ปิด trigger 9 ตาราง**ทั้งฐานข้อมูล** → เจ้าอื่นที่ใช้อยู่ตอนนั้น `edit_log` ไม่บันทึก และ 🔴 **`stock_product` ไม่ขยับโดยไม่มีอะไรมาคำนวณให้ทีหลัง = สต็อกผิดถาวรเงียบ ๆ** ไปโผล่บนฟอร์ม ภส. · ถ้า process ถูก kill กลางทาง **trigger ค้างปิดทั้ง DB ตลอดไป** · 🪤 **บทเรียน: โค้ดขัดกับกติกาที่ตัวเองเขียนไว้** — comment ของฟังก์ชันใน 0029 เขียนเองว่า *"ห้ามรันบนระบบที่มีคนใช้อยู่"* แต่ restore เป็นปุ่มที่ลูกค้ากดเองได้ตลอดเวลา · **เขียนคำเตือนข้างฟังก์ชันไม่พอ ต้องดูว่าใครเรียกมันได้บ้าง** · **ตัดทิ้งทั้งก้อน** → ปุ่มดาวน์โหลด 2 แบบ (`.json` ครบทุกตัวอักษรใช้ย้อนได้จริง · `.xlsx` ไว้อ่าน/ส่งบัญชี) + `npm run restore:tenant` ฝั่งเจ้าของ (dry-run ปริยาย ต้อง `--apply` · ปฏิเสธไฟล์ต่างกิจการด้วย `tenant.id`+`slug`) · ★ **ผลพลอยได้**: ผู้เรียก `fn_mig_set_triggers` ที่เหลือมีที่เดียวคือ `migration/import-csv.ts` ที่รันบนระบบเปล่า = ตรงกติกาพอดี (เก็บฟังก์ชันไว้) · 🚨 **กับดักที่เกือบพลาด: `fn_mig_truncate` มี `'snapshots'` อยู่** — drop ตารางแล้วไม่แก้ = **ลบ/รีเซ็ตลูกค้าไม่ได้เลย** (ตระกูล D79 · `tenantTables.test.ts` ที่ไล่อ่าน SQL คือชั้นที่จับได้) · 🚨 **สคริปต์ restore ห้ามแตะ `fn_mig_set_triggers`** ปล่อย trigger ทำงานแล้ว recompute ปิดท้าย (`edit_log` มีแถวเพิ่มจากการ restore ซึ่ง**ควรมี**) · 🪤 **PostgREST คืนแค่ 1000 แถวเงียบ ๆ** ต้องวน `.range()` — ไฟล์สำรองที่ขาดแถวคือไฟล์ที่ดูเหมือนใช้ได้จนถึงวันที่ต้องใช้จริง · 🚨 **Excel กินศูนย์นำหน้า** เลขภาษี/เลขบัตร/เลขสรรพสามิตต้องบังคับเป็นข้อความ ไม่งั้น `0105558123456` → `1.05559E+11` · 🔴 `xlsx` ต้อง `await import()` (บทเรียน pdf-lib D61) · 🪤 `RESTORE_SKIP` (`stock_product`/`profiles`) **ตัดเฉพาะตอนเอากลับ ไม่ใช่ตอนสำรอง** — ลูกค้าต้องได้ข้อมูลตัวเองครบในไฟล์ · 🚨 หน้าจอต้องบอกตรง ๆ ว่าไฟล์กดกลับเองไม่ได้ + มีข้อมูลเงินเดือน/เลขบัตร · 🔴 **เจอเพิ่มตอนเทสทางกลับจริง (migration 0050)**: (1) `fn_mig_truncate` ลบ `entities` **ก่อน** `report_runs` ทั้งที่ report_runs มี FK ชี้ไป entities → **ลบ/รีเซ็ตลูกค้าที่เคยออกฟอร์ม ภส. ไม่ได้เลย** · บั๊กมีมาตั้งแต่ 0029 ก๊อปต่อถึง 0046/0049 · 🪤 `test:tenant` ไม่จับเพราะ tenant ที่เทสสร้างไม่มีแถวใน report_runs = FK ไม่มีอะไรให้ละเมิด · 🚨 **ลิสต์ที่ "ครบ" ไม่ได้แปลว่า "เรียงถูก"** เทสเดิมตรวจแค่ชื่อครบ → เพิ่ม `ENTITY_SCOPED_TABLES` + เทสลำดับ (พิสูจน์แล้วว่าฟ้อง 0049 ผ่าน 0050) · (2) **เลขรันนิ่งถอยหลัง** — `TR-20260825` ถอย 5→1 · เอกสารที่ออกหลังวันสำรองจะปล่อยเลขกลับมาใช้ซ้ำ = ใบกำกับภาษีเลขเดียวกันสองใบ → ใช้ค่าสูงสุดของ ค่าก่อน restore/ค่าในไฟล์/max จากข้อมูล · ชุดเทส `docs/TESTING.md` ส่วนที่ 40 |
| — | **เอกสารยื่นราชการนับเฉพาะงวดที่ลงบัญชีแล้ว** (D81) | ✅ (2026-08-25) · test **629** · **ไม่มี migration** · 🎯 เจอจาก `/browser-qa` — **หน้าจอประกาศกฎหนึ่ง โค้ดทำอีกอย่าง**: บรรทัดกำกับเขียนว่า *"งวดร่างยังไม่นับ"* แต่ `getFilingYear`/`getFilingPeriod` ดึง `payroll_items` ด้วย `.like("period_id", …)` เฉย ๆ **ไม่มีเงื่อนไขสถานะงวดเลย** → ภ.ง.ด.1ก ของ tenant ทดสอบขึ้น **925,171** ทั้งที่งวดที่ลงบัญชีจริงมีแค่ **254,860** (งวดร่าง 3 งวด = 72% ของยอดทั้งปี) · เลือกงวดร่างออกใบแนบ ภ.ง.ด.1 ได้เต็มใบ · ใบ 50ทวิ ที่ยื่นให้ลูกจ้างใช้ยอดชุดเดียวกัน · 🪤 **ตระกูล D73/D75 — ไม่ใช่สูตรผิด แต่เลือกชุดข้อมูลผิด** สูตรถูกหมด golden test 28 ตัวผ่านมาตลอด เพราะฟังก์ชันบริสุทธิ์ได้ input อะไรมาก็คิดถูกตาม **คนเลือก input คือ `data.ts` ซึ่งไม่มีเทสคุม** · แก้ที่เดียว `countsForFiling()` + `keepFiledItems()` ใน `lib/payroll/filings.ts` เรียกจาก 3 ที่ (แบบเดียวกับ `periodView.ts` D75 / `empDisplayName()` D80) · 🚨 **`draft` ไม่นับ** (`post_state = {}` = ยังไม่ลงบัญชีสักขา) · 🪤 **`partial` ต้องนับ** — ลงยอดสุทธิแล้วแต่ยังไม่ลงขา WHT คือสภาพปกติของคน*ที่กำลังจะยื่น* ภ.ง.ด.1 ตัดออก = งวดที่จ่ายเงินไปแล้วจริงหายจากแบบที่ใช้นำส่งภาษีนั้นเอง · 🚨 **ห้ามข้ามงวดเงียบ ๆ** → `<DraftNote>` แถบเหลืองบอกจำนวน+ชื่อเดือนที่ข้าม (ไม่งั้นยอดแท็บ *รายงาน* ≠ แท็บ *เอกสารยื่น* แล้วหาสาเหตุไม่เจอ) · **แท็บรายงานไม่แตะ** (คำกำกับถูกอยู่แล้ว · เป็นของใช้ภายในที่ควรเห็นงวดร่าง) · 🔴 **เจอเพิ่ม: สลับชนิดเอกสารจากรายปีกลับรายเดือน แล้วใบแนบเดือนเดียวถูก render ด้วยข้อมูลทั้งปี 1 เฟรม** (React ฟ้อง duplicate key ทุกคนที่มีหลายงวด · ทำซ้ำได้ 100%) เพราะ `setLoading(true)` อยู่ใน `load()` = ทำงาน**หลัง** render ที่ `doc` เปลี่ยนไปแล้ว → `loadedKey` เซ็ตพร้อมข้อมูล + gate ด้วย `ready` 🪤 **ผูก *ข้อมูล* กับ *ข้อมูลนี้เป็นของอะไร* ไว้ด้วยกัน เช็ค `loading` อย่างเดียวไม่พอ** (กด คัดลอกตาราง/พิมพ์ PDF จังหวะนั้น = ได้เอกสารยื่นยอดผิด) · **golden test เดิม 28 ตัวผ่านโดยไม่แก้ไฟล์เทส** · ชุดเทส `docs/TESTING.md` ส่วนที่ 39 |
| — | **หน้าแก้ไขบิลต้องกรอก หมวดหมู่/งาน ของรายการได้** (D77) | ✅ (2026-08-22) · test **566** · **ไม่มี migration** · 🚨 `item_category`/`item_job` มีครบทุกชั้น (คอลัมน์ · `fn_edit_transaction` เขียนอยู่แล้ว · `buildItemInputs` ส่งอยู่แล้ว · modal โหลดเข้า state อยู่แล้ว) **ขาดแค่ช่องกรอกบนจอ** = ฟีเจอร์ที่ไม่มีอยู่จริงสำหรับผู้ใช้ (ตระกูล D74) · ตรวจด้วย build/lint/test ไม่เจอ เพราะค่าที่โหลดมา**รอด round-trip** ทุกครั้ง — ดูเหมือนถูกจนกว่าจะมีคนอยาก*แก้* · แถวที่เพิ่มใหม่หนักกว่า: `emptyItem()` ให้ค่าว่างแล้วเติมไม่ได้เลย · **โชว์ตลอด ไม่ซ่อนหลัง toggle** แบบ EntryTab (หน้านี้คือโฟลว์ซ่อมข้อมูลย้อนหลัง ต้องเห็นทันที) · ปุ่ม **เติมทุกแถว** (ทั้งบิลมักเป็นงานเดียวกัน · disabled เมื่อช่องว่าง กันล้างค่าเดิม) · 🪤 **id ของ `<datalist>` ห้ามซ้ำข้ามแท็บที่ mount ค้าง** — ซ้ำแล้วผูก `list=` เข้า element แรกใน DOM = โชว์ค่าของอีกแท็บโดยไม่มี error ฟ้อง → `edit-item-*` · การ์ด **ดู** เพิ่มคอลัมน์ งาน · ชุดเทส `docs/TESTING.md` ส่วนที่ 35 |
| — | **ชื่อในงวด = ชื่อปัจจุบัน · งวดร่างโชว์ยอดที่คิดใหม่** (D75) | ✅ (2026-08-19) · test **544** · **ไม่มี migration** · 🎯 กติกาใหม่ทั้งโมดูล: **ชื่อ = ค่าปัจจุบันเสมอ · ตัวเงิน = ค่าที่แช่ไว้เสมอ** (`emp_name` เป็น snapshot → เหลือเป็น fallback ตอนพนักงานถูกลบ) · 🚨 อาการ "คำนวณผิด" ที่ผู้ใช้แจ้ง **สูตรไม่ผิด** — D73 เลือกโชว์เลขผิดเวอร์ชัน (บังคับให้ทุกแถวที่เคยบันทึกโชว์ค่าที่แช่ไว้ → แก้ฐานเงินเดือนแล้วงวดร่างไม่ขยับ) · แก้เป็น **ลงบัญชีแล้ว→ค่าที่แช่ไว้ · ร่าง→ค่าที่คิดสด** + คอลัมน์สุทธิขึ้นบรรทัด "บันทึกไว้ …" เมื่อต่าง = **เห็นทั้งสองเลขพร้อมกัน** · 🪤 **อาการ "ตัวเลขไม่ตรงกัน" แก้ด้วยการเลือกข้างไม่ได้** ต้องแสดงทั้งคู่พร้อมบอกว่าอันไหนคืออันไหน · กฎนี้พลาด 2 รอบติด → ดึงออกเป็น `lib/payroll/periodView.ts` + เทส · 🔴 **เจอเพิ่ม: พรีวิวกับตอนบันทึกประกอบข้อมูลพนักงานคนละแบบ** — สูตรใช้ตัวเดียวกันถูกแล้ว แต่ฝั่งพรีวิวใช้ `groupCode` **ที่แช่ไว้ในแถวงวด** ส่วน server ใช้กลุ่ม**ปัจจุบัน** → ย้ายกลุ่มแล้วยอดบนจอ ≠ ยอดที่บันทึก โดยไม่มีอะไรฟ้อง → `employeeForCalc()` ตัวเดียวใช้ทั้งสองฝั่ง (server เลิก query ดิบ ใช้ `getEmployees()`) · 🪤 **"สูตรมีที่เดียว" ยังไม่พอ — ของที่ป้อนเข้าสูตรก็ต้องประกอบที่เดียว** · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.15 |
| — | **ชุดอัตราตามกฎหมาย: แก้/ลบได้ + ขั้นภาษีเลิกใช้ช่องข้อความ** (D74) | ✅ (2026-08-19) · test **534** · **ไม่มี migration** (`savePayRatesAction` เป็น upsert บน `effective_from` และ `deletePayRatesAction` มีอยู่แล้ว **แค่ไม่เคยมีปุ่ม**) · ปุ่มแก้/ลบต่อแถว + หัวฟอร์มบอกสถานะ + ปุ่มเลิกแก้ · 🪤 **ฟอร์มเคยเปิดมาพร้อมข้อมูลชุดล่าสุดรวมวันที่** → กด "บันทึก" ทับชุดเดิมทั้งที่คิดว่าเพิ่มใหม่ (คีย์คือ `effective_from`) → เริ่มที่ `blankRates()` เสมอ · 🚨 **ขั้นบันไดภาษีเป็นช่องข้อความช่องเดียว บั๊กเดียวกับ D71 เป๊ะ** → `BracketEditor` แถวละขั้น · 🪤 **ฟีเจอร์ที่ยังไม่มีทางเข้าถึง = บั๊กที่ยังไม่ถูกนับ** — เปิดปุ่มเดียวโผล่ 2 บั๊ก · อัตราภาษี**ยังเก็บเป็นทศนิยม** แต่โชว์ `= X%` ข้าง ๆ (ไม่แปลงหน่วยที่เก็บ = ไม่เสี่ยงกับเลขภาษี) · เตือนเมื่อแก้ชุดที่มีผลไปแล้ว · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.14 |
| — | **งวดจ่ายโชว์ค่าที่แช่ไว้ · ป๊อปอัพเลิกปิดเองตอนลากคลุม + Esc** (D73) | ✅ (2026-08-19) · test **534** · **ไม่มี migration** · 🚨 `PeriodTab` เคยคิดสดทุกแถวเสมอ → ลบรายการเพิ่ม 1 ตัวแล้ว**หน้างวดจ่ายเปลี่ยนทันทีแต่รายงานไม่เปลี่ยน** (กติกา "ห้ามคำนวณสด" D66 หลุดที่หน้าจอ) → แยก `live` กับ `shown`: แถวที่ยังไม่แตะโชว์**ค่าที่แช่ไว้** · แตะแล้วโชว์สด · ต่างกันขึ้น**แถบเตือนบอกจำนวนคน** (ไม่บล็อก) · `doSave` ส่งแต่ค่าที่กรอกให้ server คิดเอง = เปลี่ยนสิ่งที่แสดงไม่กระทบสิ่งที่บันทึก · 🐛 **ป๊อปอัพปิดเองตอนลากคลุมข้อความ** ทั้งแอป (8 จุด) เพราะพื้นหลังปิดด้วย `onClick` — ปล่อยเมาส์นอกช่องแล้ว click ยิงไปที่บรรพบุรุษร่วม = พื้นหลัง · 🪤 `e.target === e.currentTarget` **ไม่ช่วยถ้ายังใช้ onClick** ต้องย้ายไปเช็คที่ `onMouseDown` · `<EscToClose>` ใน `lib/shared/ui` (ไม่ใส่ให้ป๊อปอัพแจ้งค่างวดที่ต้องอ่าน) · หมวดรายจ่ายรวม 3 แหล่ง (เพิ่ม `expense_cat` ที่ตั้งไว้ในบัญชี) · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.13 |
| — | **หน้าตั้งค่า: เลิกตั้งรหัสเอง · ยืนยันก่อนลบ · หมวด/คู่ค้ามีตัวช่วย** (D72) | ✅ (2026-08-19) · test **534** · **ไม่มี migration** · ผู้ใช้ไม่ต้องคิดรหัส `a-z 0-9 _` อีกแล้วทั้ง 4 ที่ (ช่องกรอก/ตัวแปร/รายการ/ขา) — `nextCode()` สร้างให้ (`in1`/`var1`/`item1`/`leg1`) · 🚨 **สร้างให้เฉพาะของใหม่ ของเดิมห้ามเปลี่ยนรหัส** เพราะ `payroll_items.inputs`/`computed` ที่แช่ไว้อ้างรหัสนี้ (เปลี่ยน = งวดเก่ายอดกลายเป็น 0 เงียบ ๆ) · `askDelete()` ถามยืนยันครบ 7 ปุ่มลบ (เดิมกลุ่ม/ช่องกรอก/ตัวแปร ลบทันที) · หมวดรายจ่าย+คู่ค้าของขาเป็น `SuggestInput` (พิมพ์เองได้ + datalist) — **แก้ความเข้าใจผิดจาก D67** ที่สรุปว่า "พิมพ์เอง" แล้วทำเป็นช่องเปล่า จริง ๆ ต้องการทั้งสองอย่าง · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.12 |
| — | **หน้าตั้งค่าคำนวณ: แก้บั๊กกรอกไม่ได้ + รวมกล่องสูตร** (D71) | ✅ (2026-08-19) · test **534** · **ไม่มี migration** · 🔴 **ห้ามประกาศคอมโพเนนต์ข้างในคอมโพเนนต์** — `SlotPicker` เคยเป็น arrow function ใน `Variables` → ทุก setState React ได้ component type ใหม่ → unmount+mount → **`<input>` ถูกทำลายและโฟกัสหลุดทุกตัวอักษร** (ผู้ใช้แจ้งว่า "ใส่ทศนิยมไม่ได้" ซึ่งเป็นอาการ ไม่ใช่สาเหตุ) · 🔴 **ขั้นบันไดเลิกใช้ช่องข้อความช่องเดียว** ที่ parse กลับไปกลับมาทุกคีย์ (พิมพ์ `1=500, 2=300` ได้ `1=5002300`) → `TierEditor` แถวละเงื่อนไข + `sortTiers()` เรียงให้ตอนบันทึก (`tierAmount` คืนขั้นแรกที่เข้าเงื่อนไข — เรียงผิด = เงินผิดขั้น) · `<Msg>` ต้องอยู่**ในป๊อปอัพ**ด้วย ไม่งั้นโดนบัง · `multiplier` เริ่มต้น 0→**1** (เลือกวิธีคิดตัวแปรแล้วไม่แตะ = ยอด 0 เงียบ ๆ) · รวม 2 การ์ดเป็น **"สูตรและรายการคำนวณ"** แต่**ยังแยกหัวข้อคิดก่อน/คิดทีหลัง** (ตัวแปรอ้างรายการไม่ได้) · `componentFormulaText()` โชว์สูตรจริงครบ 6 วิธีคิด · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.11 |
| — | **ตัวแปรกลาง: ตัวดำเนินการ 4 ตัว + ความละเอียด · ย้ายลำดับคอลัมน์ที่กรอก** (D70) | ✅ (2026-08-19) · test **518** · migration **0044** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · ตัวแปรกลางเลือก **บวก/ลบ/คูณ/หาร** ได้ (เดิมหารอย่างเดียว) + เลือกความละเอียด **ไม่ปัด/จำนวนเต็ม/ทศนิยม 2** · เพดานขั้น 2→**3** · 🚨 **ไม่ได้ยกเลิกกติกา "ห้ามทำภาษาสูตร"** — สิ่งที่กติกาปกป้องคือ *ไม่มี parser · ไม่มีลำดับความสำคัญ · เส้นทางนับได้จนครบ* ซึ่งยังครบ · **เส้นที่ยังห้ามข้าม: วงเล็บ · ตัวแปรอ้างตัวแปร · สูตรที่ลูกค้าพิมพ์เป็นข้อความ** · 🪤 คิด**เรียงซ้ายไปขวา** ไม่ใช่กฎคณิตศาสตร์ (`ฐาน − A ÷ B` = `((ฐาน − A) ÷ B)`) → `variableFormulaText()` ใส่วงเล็บครบเสมอ + เตือนเมื่อปน +/− กับ ×/÷ · 🪤 **หารด้วย 0 ข้าม แต่คูณด้วย 0 ไม่ข้าม** (ข้ามแล้วยอดพองเงียบ ๆ อันตรายกว่า) · ค่าปริยาย `op=div` / `rounding=none` **ห้ามเปลี่ยน** (ของลูกค้าเดิมจะขยับ) · คอลัมน์ `divisors`→`steps` แต่ TS ยังอ่านชื่อเดิม = golden test เดิมผ่านโดยไม่แก้ไฟล์เทส · `pay_inputs` ย้ายลำดับด้วยปุ่ม ▲▼ (เขียน sort ใหม่ทั้งชุด) · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.10 |
| — | **เงินเดือนรอบ 2 — เอกสารยื่นราชการ** (D69) | ✅ (2026-08-19) · test **499** · migration **0043** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push) · แท็บ **เอกสารยื่น**: ภงด.1 · สปส.1-10 · ภงด.1ก · 50ทวิ · 🎯 ผู้ใช้**กรอกในเว็บราชการเอง** → กล่องยอดรวม + **ปุ่มคัดลอกตาราง (TSV)** เป็นของหลัก พิมพ์ PDF เป็นของรอง (ไม่ทำไฟล์ upload e-Filing) · 🚨 **ตัดตัวกรอง `wht>0`/`sso>0` ของระบบเดิมทิ้งทั้งหมด** — คนที่ยอดเป็น 0 ต้องขึ้นในเอกสาร (แบบถามจำนวน "ผู้มีเงินได้" ไม่ใช่ผู้ถูกหักภาษี · โรงที่ไม่มีใครถึงเกณฑ์เลยเคยได้ใบแนบว่างทั้งใบ) ยกเว้นเดียว: `ssoExempt` ไม่ขึ้น สปส.1-10 (ไม่ใช่ผู้ประกันตน ≠ เงินสมทบ 0) · **`taxableIncome` ต้องแช่ใน `computed`** ห้ามไล่อ่านธง `taxable` สดตอนออกเอกสาร (แก้ธงกลางปี = เลขที่ยื่นไปแล้วขยับ) — คืนค่าที่คำนวณอยู่แล้ว **golden test เดิมผ่านโดยไม่แก้ไฟล์เทส** · 50ทวิ พนักงานใช้**เลขชุดเดียวกับใบคู่ค้า**ต่อ entity + partial unique index กันใบซ้ำ · 🪤 `create or replace` ที่พารามิเตอร์ต่างจำนวน = **overload ตัวที่ 2** ต้อง `drop function` ก่อน ไม่งั้นฝั่งบัญชีพัง · `entities.sso_employer_no` (ไม่กรอก = ใช้เลขผู้เสียภาษี) · ชุดเทส `docs/TESTING.md` ส่วนที่ 34.9 |
| — | **เงินเดือนรอบแก้ + บั๊กดร็อปดาวน์แถบเมนู** (D67-D68) | ✅ (2026-08-19) · test **464** · migration **0042** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push — แบบเพิ่มของ) · 🎯 **ตัวแปรกลาง `pay_variables`** แทน `hourly_multiplier` ที่เคยฮาร์ดโค้ดสูตรอัตราต่อชั่วโมงไว้ในโค้ด — ตัวแปร = *ตัวตั้ง ÷ ตัวหารไม่เกิน 2 ชั้น* ทุกช่องเลือกจาก**ชุดปิด 7 อย่าง** · 🚨 **ยังไม่ใช่ภาษาสูตร ห้ามขยายเป็น expression engine** (เหตุผลเดียวกับที่ `method` เป็นชุดปิด — กติกาเหล็กข้อ 1) · 🪤 พนักงาน**รายวัน**ต้องมีตัวแปรของตัวเอง (ฐานเป็นค่าแรงต่อวันแล้ว ห้ามหารวันซ้ำ — หยิบผิดได้ 1.48 บาท/ชม. โดยไม่ error) · **ขาลงบัญชี `pay_post_legs` ตั้งเองกี่ขาก็ได้** (เดิมล็อก 3 ขาในโค้ด) หมวดรายจ่ายพิมพ์เอง · 🚨 **ขาที่ยอดซ้อนกัน = ลงรายจ่ายซ้ำ ไม่มีอะไรใน DB ฟ้อง** → `legCoverage()` โชว์ตัวเลขคุมบนแท็บงวดจ่าย (**เตือนไม่บล็อก**) · ลบช่องหลอก `pay_components.expense_cat` · **แท็บรายงาน** แยกรายการ × รายคน (อ่านจาก `payroll_items.computed` ที่แช่ไว้ **ห้ามคำนวณสด**) · **D68**: ดร็อปดาวน์แถบเมนูไม่ขึ้นเพราะ `<nav>` มี `overflow-x-auto` → กลายเป็น scroll container ตัดเมนูทิ้ง (**build/lint/test ผ่านหมดทั้งที่ฟีเจอร์ไม่ทำงาน**) แก้เป็น `flex-wrap` · ชุดเทส `docs/TESTING.md` ส่วนที่ 33.1 + 34 |
| — | **โมดูลเงินเดือน (โมดูลที่ 4) รอบ 1** (D66) | ✅ (2026-08-19) · test **437** · migration **0040** (ต้อง `npm run db:push:all -- --apply` ทุก DB **ก่อน** git push — เป็น migration แบบเพิ่มของ) · 🎯 **โค้ดเป็นกลาง ไม่มีเกณฑ์ของบริษัทใดในสินค้า** เกณฑ์ทั้งหมดอยู่ใน config (`pay_components` + **ธง 4 ตัว** taxable/sso_base/ot_base/prorate_base) · 🚨 `taxable` ≠ `sso_base` (OT/โบนัสเข้าฐานภาษี แต่ไม่ใช่ค่าจ้างของ สปส.) · `method` เป็น**ชุดปิด 6 แบบ ห้ามทำภาษาสูตร** เคสนอกเหนือใช้ `manual` · `pay_rates` = ตารางแรกของระบบที่มี **effective_from** (อัตรา/เพดาน สปส. + ขั้นบันไดภาษีถูกแก้ด้วยกฎกระทรวงเป็นระยะ) · ลงบัญชี **3 ขาแยกอิสระ** NET/SSO/WHT (post ยอดเต็มแล้วมา post ยอดนำส่งอีก = นับรายจ่ายซ้ำ) · ถอน post = **soft-void `'ยกเลิก'` ไม่ใช่ลบ** · 🪤 `payroll_items.computed` **แช่ค่าไว้ ห้ามคำนวณสดตอนเปิดดู** · `employees` แยกจาก `contacts` และ `select` เฉพาะ role `main` · **ยังไม่ทำ**: ภงด.1 · สปส.1-10 · 50ทวิ · ภงด.1ก · ชุดเทส `docs/TESTING.md` ส่วนที่ 34 |

*Definition of Done ต่อ phase อยู่ใน MIGRATION_PLAN section 12 · คำถามค้างตัดสินใจอยู่ section 11 — ถ้างานชนคำถามเหล่านั้น ให้ถามผู้ใช้ก่อน อย่าเดา*
